# Heformation：统一模型、联合求解与闭环保证

日期：2026-09-22  
版本：理论工作稿 v1  
研究对象：通信受限、状态相关执行能力下的异构机器人协同任务规划  
工程基座：D-ITAGS 派生任务规划内核；保留 Heformation 的 Swarm-Formation、qn、PVS/GNC 与执行链。

本文给出拟采用的模型、算法和条件性证明。数学命题针对下文明确的模型成立，不表示当前仓库已实现这些算法或实际 qn/PVS 已满足全部假设。它不是原 D-ITAGS 定理的直接套用，也不是新颖性已获证明的论文。

## 1. 三个不同对象

给定参数、当前收到的信息和已确认承诺，记问题参数为 θ、信息状态为 I_t、承诺为 K_t。

- 模型：可行域 F(θ,I_t,K_t)。
- 目标：J_t(Π)，评价完整协同计划。
- 算法：S(θ,I_t,K_t,budget)，搜索、评价和修复计划。

基础硬件能力 Q_i 与能力判定函数 φ_i 都是给定的；当前状态估计是输入；未来动作是否可执行，由候选计划引起的预测状态决定，不能任意作为一个可自由取真的布尔量。

I_t 不增加一个新的业务决策模块。它只是说明状态输入的合法来源：实际收到的、带生成时戳的信息。母船可根据已接纳计划预测未上报状态，但预测不等于新真值。

## 2. 索引、输入、状态与决策

机器人 i∈R，母船记为 0；作业 j∈J；方法 h∈H_j；活动 v∈V(h)；活动角色槽 r∈R_v；数据产品 d∈D；时间网格 t_k=t+kΔ，k=0,…,K。

基础参数包括：地图与工作域 W、平台能力 Q_i、原生控制器 κ_i^m、动力学 f_i^m、转换入口 G_i^{m→m'}、传感器作业谓词、可信资源消耗模型、通信设备与服务模型、缓存容量 K_i、产品大小 B_d、已知业务前置关系和硬截止要求。

状态 s_i=(x_i,m_i,e_i,b_i,η_i)，其中 x_i 是运动状态，m_i 是模式，e_i 是能源，b_i 是产品缓存，η_i 是任务/执行承诺状态。不能把同一 AAV 的 AIR 和 WATER 形态当作两台不同机器人。

决策 Π=(z,A,σ,χ,F)：

- z：方法选择。
- A：成员与角色/联盟分配。
- σ：活动开始、结束、顺序和同步。
- χ：已选运动、转换与等待方案。
- F：传输量、通信资源、路由及交付安排。

局部参考优化产生的轨迹是候选计划的一部分，不是任务层之后另行不受约束地覆盖的输出。

## 3. 初始状态与信息边界

令 B_t 表示与 I_t 一致的可能联合状态集合，含任务、模式、缓存和承诺。收到一个旧时戳消息时，应在它的生成时刻约束状态历史，再向当前传播，不能把旧测量当当前测量。

若没有新的越界事件，更新满足：

\[
\mathcal B_{t'}\subseteq\operatorname{Reach}(\mathcal B_t,\Pi_{\mathrm{committed}},[t,t'],\mathcal W_d).
\]

W_d 是声明的扰动集合，不是地图 W。单点估计是名义模型；集合估计及服务下界支持条件性的鲁棒保证。本文不把单点仿真结果称为鲁棒保证。

每次搜索固定一个 θ、I_t、K_t 快照。参数变化后旧下界、旧冲突和旧可行解要按依赖重新核验，不能直接沿用。

## 4. 目标与约束

### 4.1 目标

\[
\min_{\Pi,C} C,\qquad C\ge T_{\mathrm{end}}(\Pi)-t.
\]

T_end 要求所有必做任务、必要数据产品、业务规定的退出/返回以及必要的任务确认全部满足。不是只看观测结束或最后一个运动 Action 结束。

在名义模型中 C 是预测工期。在保证模型中约束对允许扰动与状态集合中的所有执行成立，C 是保证工期上界。二者的目标含义一致，但证据强度不同，不能混报。

用户未提供硬截止时不制造业务 deadline；有限搜索时域 H=KΔ 是算法范围，不是用户需求。当前范围无解不证明原问题无解。

### 4.2 方法与角色

对必做作业：

\[
\sum_{h\in H_j}z_{jh}=1,\quad z_{jh}\in\{0,1\}.
\]

方法激活其活动 v，记激活量 a_v。每个必须角色槽由一台机器人承担：

\[
\sum_i A_{ivr}=a_v,\quad A_{ivr}\in\{0,1\}.
\]

选中某成员角色时：

\[
A_{ivr}=1\Longrightarrow\phi_{ir}(s_i(S_v),Q_i,W)=1.
\]

角色槽可表示相机、导航、声学发送、支援或编队成员。两个角色可否由同一平台承担由资源与兼容性规定。可加性 trait 用数量和约束；不可加性的相机/工作域/通信接口必须绑定具体角色，不能拼出不存在的机器人能力。

资格检查在活动真正开始的预测状态，而不是在整个任务计划开始前做一次静态过滤。允许通过合法前置动作到达可执行状态。

### 4.3 执行动力学和动作合同

\[
\dot x_i=f_i^{m_i}(x_i,u_i,w_i),\quad u_i=\kappa_i^{m_i}(x_i,r_i),\quad w_i\in W_i.
\]

转换满足实际入口及状态映射：

\[
x_i^-\in G_i^{m\to m'},\qquad x_i^+=R_i^{m\to m'}(x_i^-).
\]

R 不能制造瞬移、速度清零或第二状态源。复杂入/出水过程可以使用显式 TRANSITION 动作。

对执行方法定义合同：入口集合 G_v、闭环管 T_v(τ)、保证完成时间上界 τ̄_v、终态集合 F_v、资源占用、真实观测产品和安全终端行为。保证版本要求在全部入口状态和声明扰动下成立。

顺序拼接要求 F_v⊆G_w；早到后的等待也必须具有合法动力学和资源/通信含义。返回之后的安全终端行为应适合具体平台，不能要求不具备悬停能力的 UUV 原地悬停。

### 4.4 时序和资源

活动前置关系：S_w≥F_v。传输和支援需要重叠时使用并行资源/传输激活约束，不用相互等待完成的环来表达。

\[
\sum_v\rho_{ivc}\mathbf 1_{[S_v,F_v)}(\tau)\le C_{ic}.
\]

资源通道 c 区分运动控制、传感器和无线/声学接口。支持边运动边通信的系统可以并发；同一成员不能接受互相冲突的两条运动参考。

编队使用真实成员集合及经验证模板。需要同一物理成员的单机动作和编队动作竞争同一运动资源。转场与等待必须计入一次，不能漏计或重复计费。

### 4.5 连续安全

若参考跟踪界为 ε_i，要求：

\[
\|p_i^{ref}(\tau)-p_j^{ref}(\tau)\|\ge r_i+r_j+d_{safe}+\varepsilon_i(\tau)+\varepsilon_j(\tau).
\]

障碍约束同样按实际几何和误差管收紧。独立局部优化器不能自行改变已承诺的跨平台安全关系。重新优化后的参考应继续满足合同或触发计划重新验证。

### 4.6 观测、传输和数据

g_{i,d,k} 为时隙内由实际合格观测产生的数据，在下一时隙可发送。其值由观测谓词和产品定义约束，不能是优化器任意增加的数据源。

先采用唯一产品分块、单一保管权、成功确认后交接的存储转发模型。时隙长度/传输边包含相应传输、处理和确认时延。较长延迟使用时间展开网络的在途边。

当一次成功传输在本时隙末成为可用数据时：

\[
b_{i,d,k+1}=b_{i,d,k}+g_{i,d,k}+\sum_j f_{ji,d,k}-\sum_j f_{ij,d,k},
\]

\[
f_{ij,d,k}\ge0,\qquad\sum_jf_{ij,d,k}\le b_{i,d,k}.
\]

时隙内的峰值缓存也要检查。一个保守充分条件是：

\[
\sum_d\left(b_{i,d,k}+g_{i,d,k}+\sum_j f_{ji,d,k}\right)\le K_i.
\]

它不在时隙末前提前利用发送释放的容量。保留的冗余副本、待确认与在途数据必须另计实际资源，不能利用删除账面条目制造虚假容量。

对共享信道的确定时隙服务：

\[
\sum_df_{ij,d,k}\le\Delta\underline R_{ij,k}(\chi,\sigma)\alpha_{ij,k},\qquad\alpha_k\in\mathcal A_{comm}.
\]

A_comm 必须是可实际执行的链路时间分配集合。单一共享半双工信道可用 α≥0、Σ_links α≤1；更复杂干扰网络不能仅凭任意几个局部不等式宣称全局可调度。

R̲ 使用所选轨迹/模式在整个时隙内的保证服务率；不是自由变量。下界松弛反而可以使用乐观服务率 R̄。执行可行性与搜索下界不能混用两者。

母船是吸收节点，对其无出边。产品交付要求 b_{0,d,K}=B_d，并且同一有效数据不能重复计数。控制命令、启动确认和必要状态结果也占对应通信资源；活动只能在满足所需接纳/本地启动条件后开始。

### 4.7 能源和承诺

启用能源约束时使用可信功率/消耗模型，包括移动、转换、等待、通信和退出，不只计算航行距离。无可信消耗上界时，只能报告该约束未获物理验证，不能给出能量保证。

已收到的动作/计划接纳作为承诺。未确认取消不得释放潜在仍在执行的机器人。对多方启动，协议应保证不会因部分消息丢失而使某成员采取不安全动作；不假定任意断联下能够原子同时启动。

## 5. 可计算的有限模型 P_Δ

理论验证先固定：有限作业/方法/角色、有限机器人、有限经验证运动/转换/等待候选、有限时域和时间网格。对每个候选规定状态入口集合、终态集合、轨迹/耗时及其适用域。

动态产生的参考必须加入并重新定义该候选问题，或属于预先声明的有限候选索引；不能一边无限生成新候选，一边声称有限搜索定理已经适用。每次信息更新是一个新的求解实例。

固定所有离散选择后，时隙化调度/数据流是有界 MILP/LP，前提是状态可执行性已通过有限候选与显式兼容约束表示；不能把非线性关系仅写在线性模型旁边却仍称整体 MILP。

保证版本有：

\[
\mathcal F_\Delta\subseteq\mathcal F_{physical},\qquad J_{physical}^*\le J_\Delta^*\le U.
\]

若只有名义执行验证，以上物理包含关系不成立，只能讨论名义 F_Δ。U−L 是相对 J_Δ^* 的界，不包含动作库限制、离散化或模型错误。窄通信窗口和模式入口会破坏普通平滑离散误差假设，不能一般声称 Δ→0 即收敛到连续全局最优。

### 5.1 为什么有限模型可以落成 MILP，而不是把非线性关系藏起来

对已验证的活动—原始动作候选 e，使用开始时隙变量 x_{v,e,k}∈{0,1}。激活活动满足 Σ_{e,k}x_{v,e,k}=a_v。若候选时长为 n_e 个时隙，S_v、F_v可分别由选定时隙及 n_e 线性计算。状态衔接通过有限“时间—模式—入口/终态集合”图中的合法弧表示。单个平台的弧流连续且只有一条轨迹；模式切换只是这条轨迹经过不同模式节点，不能生成第二条物理机器人流。

组级活动的激活要同时绑定参与成员的对应合法弧；活动资源在被占据的时隙中累加。联合运动冲突可预编译为不兼容弧对，或者由按需验证生成只对相关弧/时段有效的冲突约束。

通信容量不能直接把未知位置代入 R(χ,σ) 再乘 α，却称它线性。固定候选轨迹/时隙组合后，按有限、物理可执行的链路激活模式 λ 建立保证服务率常数 R̲^λ_{ij,k}。模式可用布尔量 u_{λ,k}由所选轨迹弧及角色决定，分配量满足0≤α_{λ,k}≤u_{λ,k}、Σ_λ α_{λ,k}≤1。于是：

\[
\sum_d f_{ij,d,k}\le\Delta\sum_\lambda\underline R^\lambda_{ij,k}\alpha_{\lambda,k}
\]

是线性的。模式集合需覆盖允许同时工作的链路集合，而不是仅假定局部信道不冲突就一定可执行。

选择一个终止时隙 γ_k∈{0,1}，Σ_kγ_k=1，C=ΔΣ_k kγ_k。对每个产品和每个候选终止时隙施加 b_{0,d,k}≥B_dγ_k；被选活动结束、规定返回及终端状态也必须在被选 k 前成立。这样不会发生“工期变量很小，数据却在计算时域最后才交付”的错误。

这是明确的有限抽象编译方式，不要求把所有可能弧提前展开存储。延迟生成可以减少计算，但证明最优时不能遗漏未生成且可能更好的弧；它们必须由未决候选与合法下界代表。

## 6. 联合求解器

选定算法结构：D-ITAGS 派生最佳下界优先搜索/分支定界 + MILP 调度/资源分配 + 时间展开通信流 + Swarm 原生轨迹优化与 qn/PVS 闭环验证 + 事件驱动修复。

这是一套联合算法，不是多个互相独立的规划器。Gurobi 是调度子问题的求解引擎，不是整个任务规划算法。运动优化器生成/改进参考；验证器只判可行性，不必本身也是优化器。

原 D-ITAGS 的 Itags 类继承 GreedyBestFirstSearch，并使用 TraitsImprovementPruning。我们需要替换其不可直接适用的下界、终止与剪枝逻辑，而不是原封不动调用然后宣称新问题全局最优。[R1,R2]

### 6.1 分解等价

令 d 表示方法、角色和运动候选等离散结构，y 表示剩余时序、通信及执行变量。定义：

\[
V(d)=\inf_{y:(d,y)\in\mathcal F_\Delta}J(d,y),
\]

空可行集取 +∞。那么：

\[
J_\Delta^*=\min_dV(d).
\]

**证明。** 每个完整可行解都属于某一个固定 d 的子问题，所以其代价不小于相应 V(d)，从而不小于 min_d V(d)。反向，对取得最小值的 d，若其子问题最优解存在，则该解属于原联合可行域并取得相同值。有限离散结构和有界闭合 LP/MILP 保证可行时最小值取得。故相等。

这解释了为什么多个模块可以联合求解同一个目标，也说明每个 d 只求一个局部/启发式结果不能自动得到联合全局最优。分解方法和有效反馈约束的基本原理见 [R3]。

### 6.2 搜索节点与下界

节点 N 代表一个“尚可补全的方案集合” Ω(N)，不是一个已经固定不再改变的低质量计划。分支覆盖应满足：

\[
\Omega(N)=\bigcup_{M\in child(N)}\Omega(M).
\]

根节点覆盖整个 F_Δ。角色、路线、时序以及尚未精确求解的子问题，必须全部由某个未决节点代表。

构造松弛 R(N)⊇Ω(N)，定义：

\[
L(N)=\min_{\Pi\in R(N)}J(\Pi).
\]

可以放松整数性、资源互斥、链路竞争，使用合法的最短耗时和最高速率界；不能用猜测的行程时间当合法下界。零下界虽弱，但在非负工期下总是合法。

**下界引理。** 对任何该节点合法补全 Π，L(N)≤J(Π)。证明由可行域包含直接得出。

上界由一个完整且通过统一校核的计划产生：U=J(Π_inc)。MILP 未求完时，feasible incumbent 是子问题上界；求解器的有效 dual/best bound 才能用于下界。

一个可达计划耗时并不等于节点下界。特别是增加中继可能降低交付时间。原 D-ITAGS 工期定理使用“增加分配不降低工期”的特定性质，不能照搬。[R1]

### 6.3 正确剪枝

只使用：

- 已证明 Ω(N)=∅。
- L(N)≥U。
- 已证明的支配关系。

若布尔文字 ℓ_1,…,ℓ_p 共同成立会导致确定冲突，可加：

\[
\sum_{q=1}^p\ell_q\le p-1.
\]

冲突必须带齐有效前提。某条路线在某时段失败，只能排除相应路线/时段组合，不能排除该机器人所有路线或整个任务分配。保守轨迹管冲突可以证明不属于当前保守 F_Δ，不等于证明原连续问题绝无解。

局部运动优化未找到解、超时、状态未知均为 UNKNOWN。只能延后/扩大查询，不能凭此生成不可行证明。

### 6.4 伪代码

```
输入：固定问题快照、有限候选范围、搜索预算
OPEN ← {root}; U ← +∞; incumbent ← none
将仍有效的旧完整计划核验后作为 incumbent

在预算内公平处理 OPEN 与未决子问题：
    选取具有最小有效下界的可处理节点 N
    求解/加强 N 的时序—通信松弛，更新 L(N)
    如果 L(N)≥U：剪枝
    如果有覆盖 N 的不可行证明：剪枝
    否则按需查询状态相关运动与转换可执行性
    如果 UNKNOWN：保留其分支与下界，不判无解
    如果有完整可行计划 Π：U ← min(U,J(Π))
    如果 N 还有可能产生更优补全：继续分支或恢复子问题
    仅在 N 已被精确求解、证明剪枝或被子节点完全覆盖后删除 N
    计算整个未决边界的最小下界
    如果 U−L_global≤ε：返回 ε-最优证书

预算结束：返回已核验 incumbent、上下界、未决状态和适用范围
```

公平处理防止一个持续 UNKNOWN 节点阻塞其他节点。精确终止定理额外要求每个必要查询最终得到决定；否则算法可以保持安全的 anytime 结果，但不能保证有限时间证明最优。

## 7. 定理：可行性、最优性与有限终止

### 定理1：模型内可行性

假设每个输出计划都通过全部激活约束的统一验证，并且所用执行合同、资源峰值模型和通信服务下界有效。则输出计划满足 F_Δ；在动作合同覆盖真实扰动时，满足所声明的物理任务与安全条件。

**证明。** 角色和激活约束确保所选方法合法。初态属于首动作入口；每个动作保证其过程管、完成时间及终态，终态包含于后继入口，因此沿各成员活动序列归纳得到模式与动力学衔接。并行活动由统一资源/轨迹/链路约束联立验证，不从单个可行推断组合可行。队列不等式禁止提前转发；接收终止条件保证数据交付；承诺条件禁止未确认切换。等待、转换和返回均被纳入，所以不存在未建模的拼接区间。若仅有名义合同，结论只限于名义模型。

### 定理2：数据因果与守恒

假设初始每个产品有效块唯一计数，g 是合格观测的真实生成，f 为成功的去重交接，则在每个时隙：b_i,d,k≥0，且母船不能在产品产生前获得产品。

**证明。** 由 Σ_j f_ij,d,k≤b_i,d,k 及其他项非负，递推得 b_i,d,k+1≥0。对 i 求和，全部发送与接收相消：

\[
\sum_i b_{i,d,k+1}=\sum_i b_{i,d,k}+\sum_i g_{i,d,k}.
\]

因此系统不会凭空生成数据。若有跨多时隙传播，等式左、右加入在途数据即可。母船只从有效数据保管权交接获得产品，故不能提前交付。这个定理是正确性，不单独构成算法创新。

### 定理3：上下界与 ε-最优性

假设根覆盖 F_Δ、分支不漏解、下界有效，且所有未解决子问题包括 UNKNOWN 都在边界 F 中保留。对有限 U 定义：

\[
L_g=\min\{U,\min_{N\in\mathfrak F}L(N)\}.
\]

边界为空时 L_g=U。则：

\[
L_g\le J_\Delta^*\le U,\qquad0\le U-J_\Delta^*\le U-L_g.
\]

**证明。** U 来自完整可行解，故 J_Δ^*≤U。若最优解未被已核验 incumbent 覆盖，则至少由一个未决节点代表。该节点的有效下界不大于它的代价。因有效剪枝不会去掉严格优于 U 的解，取全边界最小值得 L_g≤J_Δ^*。两式相减得到误差界。

因此 U−L_g≤ε 时返回 ε-最优；等于0时为当前有限模型最优。不能拿一个局部子问题的 gap 代替整个边界的 gap。

在同一问题快照内，保留 incumbent 使 U 单调不增。事件改变模型后这个单调性不跨实例成立。

### 定理4：有限候选内的有限终止

额外假设离散决策有限、每个必要验证或子问题在有限时间内精确决定、搜索公平且不重复无效处理同一子问题。则无限预算下算法在有限步内找到 F_Δ 的最优解或证明 F_Δ 为空。

**证明。** 完整离散结构数有限。每次分支固定至少一个尚未决定的有限变量；每个叶子经过有限时间处理后被精确解决或剪枝。若有更优解，它不会被合法剪枝误删，故必有叶子取得它；处理结束后所有剩余边界下界均不优于 U。无可行叶子则 F_Δ 为空。

这是终止保证，不是多项式时间保证；最坏情况仍可能指数级。

## 8. 从控制误差推导安全与信息时限

### 8.1 跟踪界

若某模式已证明：

\[
\underline a\|e\|^2\le V(e),\qquad\dot V\le-\lambda V+\sigma\bar w^2,
\]

则积分得：

\[
V(t)\le e^{-\lambda t}V(0)+\frac{\sigma\bar w^2}{\lambda}(1-e^{-\lambda t}).
\]

因此可取相应位置误差上界 ε(t)≤sqrt(V_bound(t)/a_under)。转换时必须核对新的 Lyapunov/误差合同以及入口集合，不能从单模式稳定直接推出任意跨介质切换稳定。

真实 qn/PVS 是否满足上述不等式必须由实际方程、控制律和参数验证，不在本文假定“已经完成”。规划参考与跟踪误差界的区分可参见 FaSTrack [R5]。

### 8.2 安全传递证明

由三角不等式：

\[
\|p_i-p_j\|\ge\|p_i^{ref}-p_j^{ref}\|-\|p_i-p_i^{ref}\|-\|p_j-p_j^{ref}\|\ge r_i+r_j+d_{safe}.
\]

所以收紧后的参考约束足以保证实际净距。

若只有间隔 h 的几何样本，还必须有连续速度界。设距离函数 Lipschitz 常数不大于 L_d，则区间两端均至少高于阈值 L_d h/2，可保证整个区间不越阈。证明选取最近端点，其时间距离≤h/2。没有速度/导数界不能用采样安全证明连续安全。

### 8.3 信息年龄上界

若在声明域内，远端状态预测误差满足：

\[
\dot\varepsilon\le L\varepsilon+\bar w,\quad\varepsilon(0)=\varepsilon_0,
\]

则对 L>0：

\[
\varepsilon(\tau)\le\varepsilon_0e^{L\tau}+\frac{\bar w}{L}(e^{L\tau}-1).
\]

令当前动作允许的预测不确定性为 δ，且 δ≥ε0，则充分条件：

\[
\tau\le\tau_{max}=\frac1L\log\frac{\delta+\bar w/L}{\varepsilon_0+\bar w/L}.
\]

L=0 且 w̄>0 时，τmax=(δ−ε0)/w̄。若初始误差已超 δ，动作不能靠等待恢复资格。若 L=0 且 w̄=0，模型本身不导致误差增长，但仍需满足其他任务与通信条件。

年龄计算为当前时刻减消息生成时刻，不是减最后接收时刻。接受一条很旧的消息不一定重置安全年龄。δ 应来自具体运动/任务余量，且不能重复计算跟踪误差和预测误差。

任务与运动→可接受不确定性→消息接收需求→通信时序→重新选择方法与任务，是本问题的实际耦合，不要求增加独立“信息决策器”。

## 9. 局部修复、递归可行与任务进展

### 9.1 局部修复一致性

每条约束记录其依赖的变量与参数。事件发生后，只能无条件复用输入及前提均未改变的验证结果。凡是跨越修改区和冻结区的约束都要重验。

**局部修复命题。** 若未重验的约束所有依赖保持不变且原来成立，全部变化及跨边界约束已重新成立，则组合后的计划可行。

**证明。** 对任意约束分类：未变化者仍取原值，故成立；其余已重新验证，故也成立。所有约束成立即属于新的可行域。

冻结未执行决策会缩小可行域；局部最优不是全局最优。局部修复失败只说明该冻结条件下未找到/不存在解，应扩大修复范围而非直接宣布整个任务无解。共享声学信道等全局资源可能使局部影响扩展到大部分任务。

### 9.2 递归可行定理

假设：

A. 当前保留一份在状态/扰动和通信服务界内鲁棒可执行的完整计划或带本地反馈的执行策略。

B. 真实执行没有超出该合同；新状态集合包含于旧计划可达集合，通信服务没有低于保证下界。

C. 原任务要求未增加；新问题的候选表示保留原计划的剩余段，包括正在执行的不可中断段及其必要等待。

D. 全部已接纳承诺、接收条件、缓存和绝对截止均保留。没有新的可行解时继续合法的旧计划剩余段，不因求解器超时清空计划。

则在下一次更新时，剩余任务规划至少有一个可行候选。

**证明。** 取旧计划已经执行前缀之后的剩余策略。由A、B，新可能状态是其原先保证覆盖的状态子集。由C，这个剩余策略仍可由求解器表示。由D，它仍满足所有未被完成事实消除的约束及承诺。因此该剩余策略是新问题的一个可行解。

这是“旧可行方案剩余段”证明，不是从“会重新求解”推出可行性。若仅使用固定长度滚动时域，还要有可延拓终端策略/终端集合，不能丢掉保证必需的后缀。[R4]

### 9.3 有限完成推论

额外假设旧策略有保证的绝对完成时刻 D0<∞，每次改计划只接受保证完成时刻不晚于当前证书的方案，执行持续推进且不存在有限时间无限切换。则任务最迟在 D0 完成。

设 W_n 是时刻 t_n 被接受策略的剩余工期保证。旧策略剩余段给出：

\[
W_{n+1}\le W_n-(t_{n+1}-t_n).
\]

累加得到：

\[
W_n\le W_0-(t_n-t_0).
\]

由于剩余工期非负，时间达到 t0+W0 时必须已经进入任务终止集合。这是对任务完成的进展证书，不是用一条机械系统 Lyapunov 函数证明所有业务逻辑。

如果永久失去唯一中继、发生超出模型的故障或新增必做任务，B/C可能失效，不能仍承诺任务完成；保守锁定和安全退出也不等于剩余业务任务可行。

## 10. 可手算的小实例

这是数学构造例，不是 Heformation 仿真结果。

三个 AAV 有一个必须共同执行的编队活动，工期30秒，已包含其规定返回。另有水下产品20KiB。只有以下三种水下方法：

1. UUV独立方法：完整观测、交付、返回需90秒。
2. AAV1跨介质方法：完整水下方法需35秒；与30秒编队活动争用AAV1，因此总工期至少65秒，并可串行达到65秒。
3. UUV+USV方法：UUV在20秒生成产品，USV在25秒具备支援条件。声学接收和转发共享一个半双工资源，两跳速率分别4和2KiB/s，USV在最后转发后返回需10秒。

第三种方法通信时间至少：

\[
20/4+20/2=15\ \text{s}.
\]

因此终止不早于25+15+10=50秒。可行安排：25–30秒USV接收，30–40秒转发，40–50秒USV返回。UUV在30–40秒返回，三AAV在0–30秒编队执行。50秒下界可达到。

所以该有限问题全局最优值为 min(90,65,50)=50秒。增加USV分配从90降到50，表明不能以“增加分配必使工期上升”作为扩展问题的普遍依据。

## 11. 源码落实的必要变更

- Itags/搜索节点：保留分配图机制，扩展方法、角色、模式/状态和数据前提；搜索键不能只有分配矩阵。
- 搜索终止：不能只判断 trait 缺口归零，还要得到完整、可执行、可交付并满足返回的计划。
- TraitsImprovementPruning：不再无条件剪除未减少原 trait 缺口的支援/转换步骤。
- 调度器：加入实际资源、方法激活、数据时序与共享通信容量，并区分可行值与有效下界。
- 运动查询：限定初态、模式、开始时刻、其他已承诺参考与版本；UNKNOWN保留；不可直接继承只按种类和几何起终点缓存的前提。
- 修复：重验受影响任务及其跨边界约束，旧上下界按快照失效规则更新。
- 执行：只执行已接纳计划；真实收到的事件进入上层；原始真值仅用于本机控制和独立评测。

现有 Heformation 的 ExecutionCandidate、ExecutionStep、terminal_states、activities、generated_products 可以作为执行侧载体复用。复用载体不等于继续使用Calvo求解。[R7]

## 12. 本稿证明了什么，尚待哪些真实系统证据

已给出一般性证明：分解等价；模型内可行性；数据守恒；上下界与ε-最优；有限候选内有限终止；安全传递与信息年龄充分条件；局部修复一致性；在明确假设下的递归可行和有限完成。

尚需用本项目方程/数据建立：qn/PVS实际动作的入口/终态和误差合同；跨介质控制适用域；连续几何验证；包含延迟和确认的通信服务下界；实际资源峰值；算法实现满足分支覆盖、有效剪枝和UNKNOWN处理。

没有证明：全部连续物理策略的全局最优、面对任意永久故障的任务完成、任意通信黑障下的多方原子同步、算法多项式时间、研究首次性。上述一般优化证明是严谨基础，不应单独包装为新的机器人控制理论。

## 参考来源与阅读边界

[R1] Neville et al., D-ITAGS: A Dynamic Interleaved Approach to Resilient Task Allocation, Scheduling, and Motion Planning. 本轮核查原文任务/调度结构、Section VI工期界及对应PDF页面。https://arxiv.org/abs/2209.13092

[R2] 官方源码，Itags继承GreedyBestFirstSearch及TraitsImprovementPruning的实现。本轮直接读取GitHub文件。https://github.com/gneville6/D-ITAGS/blob/main/include/grstapse/task_allocation/itags/itags.hpp ； https://github.com/gneville6/D-ITAGS/blob/main/include/grstapse/task_allocation/itags/traits_improvement_pruning.hpp

[R3] J. N. Hooker, Logic-based Benders Decomposition for Large-scale Optimization, 2019. 本轮核查分解基本理论、上下界和有限主变量情形的终止结论。https://arxiv.org/abs/1910.11944

[R4] D. Q. Mayne, M. M. Seron, S. V. Raković, Robust model predictive control of constrained linear systems with bounded disturbances, Automatica 41(2), 2005, 219–224. 本轮核对出版社摘要；本文的混合任务递归可行证明单独列明假设，不声称该线性MPC论文直接覆盖Heformation。https://doi.org/10.1016/j.automatica.2004.08.019

[R5] Herbert et al., FaSTrack: a Modular Framework for Fast and Guaranteed Safe Motion Planning, CDC 2017. 本轮核对作者arXiv摘要，作为规划与跟踪保证分离的来源，未继承其控制器或误差界。https://arxiv.org/abs/1703.07373

[R6] Guo and Zavlanos, Multi-Robot Data Gathering Under Buffer Constraints and Intermittent Communication. 本轮核对arXiv条目，作为有限缓存与间歇数据收集的问题背景；本稿队列式是明确定义的工程模型。https://arxiv.org/abs/1706.02092

[R7] CquL/Heformation, integration/mrta_python/models.py. 本轮直接读取已有执行数据结构，不代表迁移已实施。https://github.com/CquL/Heformation/blob/main/integration/mrta_python/models.py
