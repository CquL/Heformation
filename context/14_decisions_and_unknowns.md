# 当前冻结决定与未知项

> **2026-09-24 新实证与两项待答**：原港口REMUS方法和预承诺Otter支援已在正式水下组件同次实跑，原业务样点/回区不变；但完整能力的普通两区域请求按现有几何代理与工期会选AAV替代、UUV待命。三类集成测试暂不提供AAV水下方法时虽然求得完整AAV＋UUV＋USV计划，360/180s规划期均使AIR原时间基线失效，不能据组件通过宣布整场通过。仍需用户明确：最终代表业务是否要求UUV独有水下结果资格（不得用强制出动奖励代替）；初次完整规划可否在物理任务时钟前离线使用长预算，或必须保留严格10s。在线修复原10s/真实缺测复查仍是未完成项。具体证据见WORKLOG顶部与`context/02`。

> **2026-09-23 新决定：REMUS保留原港口任务几何**。用户选择此前示范几何问题的方案2：只保留原水下样点与原部署/回区，继续查原生可行路线，不另列改变样点/回区的演示请求。原`water_sample=(0,8,-2)`、回区`(-5,8,-2)`、0.805621m半径及原实体余量不变。最新有限PVS核查仍失败（最近1.252803m且quay越限），见WORKLOG顶部；不能因此称全空间无解。**初次规划预算是独立的第二项问题，尚无用户答案**；此前下方“示范几何是否允许改变仍待答”已由本决定覆盖。

> **2026-09-23 当前口径（覆盖下方历史阶段表）**：现有`joint_request`正式runner已在固定港口、声明初态资格范围内同次完成AAV2 AIR＋USV支援、AAV1跨介质水中两区域任务、有限收件与规定返回；中文RViz/任务面板实时运行，静态云单条bag＋动态执行bag的独立五平台审计全项通过。UUV在这次普通请求待命，因此**最终三类平台实际协同仍未完成**；REMUS现有样点回区失败、真实缺测后再分配、在线内部状态查询/修复及默认10秒规划仍是当前未知/待办。真实设备通信与载荷质量未标定。下面2026-09-19/20的“USV/UUV offline-only”“受限交付未实现”“新代码未提交”均为历史时点，不是当前事实；最新证据以`context/02_current_status.md`与WORKLOG顶部为准。

> **REMUS返回用户决定与只读候选（早期记录）**：返回以观测后重进明确部署区、随后完成原生安全尾段为准，不要求在回区内悬停，资源直到尾段合法终态才释放。现有港口原水下样点/部署区仍无法通过；另一个在内存中改变样点与部署区的有限双会合候选已核原PVS/障碍/通信和采样两船净距，但没有改变业务配置或ROS实际执行。详见WORKLOG顶部。该时点示范几何曾待答，现已由上方方案2决定：不得改原港口业务几何；只有初次规划预算策略仍待用户答复。

> **2026-09-20协同设计复核**：用户要求AAV何时入/出水、任务选谁、何时使用编队、USV何时何地支援由优化决定；业务无截止不强制输入deadline。模式能力来自平台资格，禁止入水仅为特殊外部限制。VRX不是主线前置，优先现有共同障碍场景完成一项真实协同任务。
> 文献依据、约束与减封装路线见[协同设计依据](../docs/requirements/cooperation-source-design-20260920.md)。目前无证据证明编队必然提升监测收益，不加任意奖励强迫选择；编队能力验收保留并调用原Swarm。继续使用现有runner/Action，不新增协调管理器或消息信封。本次研究更新不表示恢复已暂停的实施目标。

**更新：2026-09-19**

本轮冻结G0–G5覆盖旧阶段范围：允许水下通过式观测、qn水下/转换隔离修正、系统实跑与条件性论证。
当时Qt与异步AIR基础、隔离跨介质与PVS资格已有实跑；当时完整五平台与受限交付仍未实现（当前范围见顶部更新）。
参考/实际介质/执行阶段分开；通过式动作必须有终端行为；断联前预承诺通信机会；每次规划共享有限预算。
详见 `docs/requirements/five-platform-implementation.md` 与进度报告；不得将以下历史“已冻结”理解为禁止本轮新增范围。

## 1. 已冻结项目决定

| 决定 | 当前状态 |
|---|---|
| 最终平台组成 = 3 AAV + 1 USV + 1 UUV | 已冻结 |
| 七机不再代表最终系统 | 仅 M1/M2、历史兼容和回归 |
| 当前先完成近岸监测 AAV 子系统 | 已冻结 |
| 首版在线只执行 AAV | USV/UUV offline-only |
| 三单机 Executor + 一三机 Formation Executor | 已冻结 |
| Executor 可静态共享成员，但不得同时 ACTIVE | 已冻结并已有计划层实现 |
| 单机目标 = member world target | 已实现 |
| 组级目标 = formation centre + slot | 已实现 |
| 三机使用声明式 3 节点期望编队图 | 已实现并实跑 |
| 单机/组级模式必须可反复切换 | 已实现并实跑 |
| 任务线串行基线与显式并行试验 | 默认串行保留；0.35m/s受限并行完整请求通过，1.5m/s失败仍保留 |
| 成员预测状态按物理成员维护 | 已实现并单测 |
| Observation 与 Delivery 分离 | 已实现 |
| C_delivered 必须 observed AND received | 已实现 |
| 条件复测最多一次 | 已实现语义 |
| 固定走廊、回退、朝向及未标定图像评分 | 已撤销，不作为活动判据 |
| 用户确认后才开始执行 | main@1840b08 已接线并实跑 |
| Swarm-Formation 保留 AAV 编队后端 | 已冻结 |
| 只推 main，不自行扩大范围 | 已冻结 |
| 每次代码/实验更新 WORKLOG | 已冻结 |

## 2. 已验收基线与后续范围

runner Executor、多端点真实派发、CLI确认、完整几何代理请求及任务权威显示已在 `1840b08` 验收并推送。
本轮需求基线与本地固定参考停止保持已取得实施报告中的正负例及回归证据；新代码未提交。以下仍属后续范围：
- USV 水面后端；
- UUV 水下后端；
- RF/水声通信；
- 通信窗口/缓存/中继联合安排；
- 能量联合约束；
- AAV 水中/跨介质模式。

## 3. 当前已知技术未知

### 同位置目标的 adoption 证据

已解决：适配分支复用原生静止多项式并保留轨迹 ID 递增；qn 的实际采用证据仍必须成立。不能把旧问题再列为待开发。

### 本轮已确认范围

采用分层监测作业，编队可选、同类 AAV 可替换。先做取消和局部点云失效后的本地固定参考保持，整链重启恢复。
需求及任务书编成／指标差异见 `docs/requirements/layered-monitoring-and-safety-hold.md`；不以本轮代理验证代替五平台最终验收。

### USV/UUV 后端

目标架构明确，但具体在线后端还未冻结。

Stonefish / VRX / DAVE 等是候选，不同时全部接入。

### 通信模型

真实设备参数未知。

第一版允许零延迟 delivery 代理，但进入通信研究前必须明确：

- RF / acoustic；
- delay；
- bandwidth；
- loss；
- connectivity；
- relay role。

### 新理论

任务—信息—运动联合、角色选择和通信 deadline 仍是候选问题，不是已定创新。

## 4. 已撤销或过时的决定

以下不再作为当前方案：

- “固定七机就是当前目标平台系统”；
- “多 Executor 只允许离线且不得成员重叠”；
- “最终异构平台组成后续再定”；
- “z 固定为 0.5 m”；
- “所有任务通过单一 formation_action endpoint”；
- “任务启动后自动读取固定 tasks 就开跑”；
- “观测完成等于到位+驻留”。

历史资料可以保留，但不能重新作为默认执行规则。
