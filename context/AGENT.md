# AGENT.md — 海上异构无人集群项目

> 项目背景包版本：2.0｜2026-09-15。以此次V2调整为依据。本文是AI工作入口，不是已实现系统或理论验收报告。

## 1. 项目做什么

建立**母船统筹、各航行器独立执行的海上异构无人集群任务与编队作业系统**。输入结构化任务、平台/资源配置、已知地图及实际可用状态，完成任务分配、资源与时间安排、必要的协同/编队、路径与轨迹规划、平台相关控制，以及执行反馈和计划修复。

协同是为了完成任务，不是要求所有类型的航行器始终同步、保持同一个队形。第一版用一个近海多区段巡检/调查场景串起完整闭环，场景中有多项子任务，而非一个固定目标点。

**不做业务图像或声呐识别、珊瑚/风机缺陷诊断、业务载荷选型、SLAM及定位算法研发。**作业要求由外部输入。传感器未知不阻塞任务控制研究，也不能据此虚构载荷、定位或网关能力。

## 2. 此次调整的硬边界

**旧HUC退出新工程。**不再读取、审计、推荐继承旧 `CquL/heterogeneous-unmanned-cluster`；不以其目录、Sxx阶段、Envelope、状态机或接口为新系统基础。旧资料中的“先做HUC的P0盘点”已废止。只识别新工作区和选定上游，不把旧项目审计当作任何前置条件。

**先复现，再最小集成，再研究创新。**优先采用有论文与源码依据的上游成果。保留原生Task/Action、状态、目标、轨迹和反馈；只在确有语义差异时写薄适配。不预先建立庞大的任务合同、证书、缓存权限或统一数据包体系。

**第一版就保留完整联合规划协调器。**必须有“任务分配/时序 ↔ 运动可行性、旅行时间或实际执行延迟 ↔ 修复/重新安排”的实际反馈。独立复现是实施步骤，不是把任务层从第一版目标中删除。

**创新不提前定案。**过去提出的任务—信息依赖、通信资源化、控制反向约束，仅是候选研究方向。先发现并复现问题，再对照已有方法，明确自己的模型、算法、假设与证明。

## 3. 当前事实与上游角色

用户已确认的复现基础是**Swarm-Formation**。本资料不推断其本地路径、commit或性能指标；新增成果尚无用户本地运行证据，不能写成已经复现。

- 优先新增复现：**Primitive-Swarm / Primitive-Planner（T-RO 2025）**。它是UAV多机导航与避让后端，不是严格编队算法的直接替代品。
- 上层：先分别做**Calvo/Capitán的资源调度与配套执行系统**、**D-ITAGS**的小例子，按任务需求冻结**一个在线主求解器**。能源/长时工作优先时Calvo是V2默认候选；任意任务前置关系需统一优化时优先核对D-ITAGS。不得把两者能力自动相加。
- Swarm保留已复现编队基线；T-RO 2023完整后续源码覆盖尚未核实。CAT-ORA仅按适用假设处理编队重排。
- RMADER、DMPC-Swarm、AMSwarmX为针对性对照，不是必须顺序串接的规划层。RSS 2025 H-LTL/GCS、CoCoPlan等用于模型与查新对照。
- 海面/水下模型参考Fossen及Stonefish；网络联合仿真按需评估DANCERS。尚不能声称这些与上层、UAV后端已经现成集成。

详细来源及能力边界见`context/12_literature_review.md`、`13_references.md`。

## 4. 每次如何读取背景

先读本文件、[当前状态](context/02_current_status.md)、[最新交接](context/15_handoff.md)。随后只补读本次需要的部分：

| 工作 | 文件 |
|---|---|
| 系统与最小连接 | `context/03_system_architecture.md`、`04_inputs_outputs.md`、`07_reproduction_integration.md` |
| 选型/原始复现 | `context/12_literature_review.md`、`13_references.md` |
| 状态与通信 | `context/05_state_communication.md` |
| 场景与实验 | `context/08_first_scenario_benchmark.md`、`09_implementation_plan.md` |
| 理论与原创性 | `context/06_dynamics_and_theory.md`、`10_research_novelty.md` |
| 范围、术语与决定 | `context/01_project_background.md`、`11_glossary.md`、`14_decisions_and_unknowns.md` |

常用任务正文在[prompt/](prompt/README.md)。`context/sources/`仅为来源快照，不必每次全读；旧V1背景不属于当前工作指令。

## 5. 最少但不可省略的集成检查

一次任务只有一个在线主调度器；同一平台同一时刻只有一个生效的运动后端和一个权威动力学状态源。不要让两个原始模拟器同时推进同一机器人。

沿用上游接口，但核对ID、坐标/单位、时间、命令层级、开始/完成/取消含义。先做“任务动作→原生目标”和“实际结果→原生反馈”两个方向的适配；没有依据不新增接口。原局部规划器没有旅行时间查询服务时，不虚称已有该能力，可先使用原估计和实际延迟修复。

本机可以经本机状态接口读取自己的仿真状态；邻机信息只能来自实际收到的消息。已知地图可预装。受限通信测试不得从世界真值、显示或共享内存绕过远程消息限制。

平台模型不能混用：不把调小UAV速度当作UUV动力学；同一AAV不同时占用两种身份；不默认欠驱动平台能悬停或任意侧移。理论保证只在对应模型与假设下使用。

## 6. 工作方式与交付

先完成选定上游的原始小例子，保存必要的版本、配置、命令和结果；再做集成。不要同时迁移ROS/系统、修改算法、重写接口和更换动力学。最终环境尚未冻结，不默认沿用旧HUC的ROS 2环境，也不默认所有新论文的软件都很新。

不因需要证据而堆积大量必填表或复杂运行日志。一次工作记录清楚来源、改动、实际命令/结果、失败原因和下一步即可。可解问题用工具解决，不重复追问已确认方向，也不以无限调研替代已授权实施。

保护现有未提交修改；不擅自删除环境、重置上游、覆盖其他项目规则、强推或启动实物。许可文件、密钥不写入资料或提交。测试未执行就写未执行，求解器超时不写成数学无解，复现与集成成功不自动写成理论证明。

本轮文献条目来自V2既有记录，并非持续自动核验的最新状态。真正需要更新选型时，再查原论文、作者/会议页面与官方源码，保留发表状态和假设；第三方issue不是定理证据。

完成工作后更新`context/02_current_status.md`与`15_handoff.md`；改变选型或范围时同步`14_decisions_and_unknowns.md`。只更新实际发生的事实。用户后来明确的新决定应同步替换相应规则，不保留互相矛盾的“当前方案”。
