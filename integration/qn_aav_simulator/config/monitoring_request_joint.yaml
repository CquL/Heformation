# Business geometry for the joint nearshore mission. No assigned platform,
# conversion site or USV departure time. Native observation/transport can load
# this now; the legacy AIR-only expander correctly refuses the full request.
request_id: nearshore-joint-20260920
service_time_s: 4.0
deadline_s: null
delivery_required: true
# System policy for this representative request: every dispatched member must
# reach its accepted starting location; a failed native return leaves it open.
return_required: true
# Explicit geometric proxy, not a calibrated camera/sonar quality model.
requirement:
  footprint_radius_m: 2.5
  min_dwell_s: 1.0
  cruise_altitude_m: 0.8
  max_distance_from_altitude_m: 3.0
regions:
  - region_id: overview
    kind: SURFACE
    corner_a: [-29.0, 3.0, 0.0]
    corner_b: [-27.0, 5.0, 0.0]
    interest_points:
      - {point_id: air_sample, position: [-28.0, 4.0, 0.0], weight: 1.0}
  - region_id: seabed_samples
    kind: UNDERWATER
    corner_a: [-1.0, 7.0, -2.0]
    corner_b: [1.0, 9.0, -2.0]
    interest_points:
      - {point_id: water_sample, position: [0.0, 8.0, -2.0], weight: 1.0}
