# Heformation 统一建模与实施方案 v2

日期：2026-09-22。代码核查基线：`CquL/Heformation@8274cd73039cdc37e9f0980f785936894d3de508`。

本文是建议采用的模型、求解算法与实施规范。它不代表已经修改仓库，也不代表 qn/PVS 的全部物理安全条件已经得到证明。它替代前稿中“迁移 D-ITAGS 内核”和“单一保管权数据守恒作为现有实现证明”的表述。

## 1. 固定研究问题与工程路线

研究问题定义为：**面向通信受限空—海—潜异构无人集群的状态相关能力约束任务分配与协同调度。**其中“海”指水面工作域，具体平台按已配置且已验证的工作模式区分，而不是仅按名称授予能力。

给定作业目标、平台基础能力、模式相关的闭环执行模型、地图、通信政策、实际收到的信息和已接受承诺，联合选择执行方法、成员角色、活动时序和运动/支援安排，使必要观测、必要数据交付及规定返回完成，并在新信息到达后修复受影响的未承诺计划。

路线固定为：**现有 Heformation 工程 + 完整候选联合搜索 + HiGHS 数值子问题 + 原有 Swarm/qn/PVS 执行链。**不迁移 D-ITAGS 整库，不新增第二个任务调度服务。Calvo v9 派生的旧排序不作为新协同任务的唯一决策规则或自动回退。旧算法通过固定版本进行离线对照，保留来源署名和历史证据。

Calvo 原方法已共同处理异构分配、联盟、时序及执行修复；不是“原先完全不联合，现在才联合”。我们的增量是扩大共同决策的变量与约束，并让候选评价、接受和修复在同一语义下运行。[R1] D-ITAGS 的交错搜索与修复作为机制参考，不直接继承其名称、剪枝适用性或最优性结论。[R2]

## 2. 用户输入与期望运行效果

用户输入业务目标，例如“完成指定近岸区域的空中与水下监测并形成监测结果”。详细的观测标准、结果类型、接收端、返回原则和最多一轮缺测复查，来自经过确认的任务模板、平台配置和运行政策；用户有明确特殊要求时再覆盖。系统不能任意创造业务要求，也不应每次把传输字节数、选哪台机器人、谁入水、谁中继、何时编队等优化决策变成必填项。

系统读取实际掌握的状态，为每项作业展开合法方法，联合比较成员、模式、支援路线、时序和结果交付；生成一份计划后经确认和节点接纳执行。平台本地用自己的真实状态控制运动，按实际观测产生数据；母船仅依据真正收到的结果更新任务。已收到的缺测终结报告可触发一次复查，单纯没有收到消息不能推断缺测。最后确认必要结果及返回条件满足，才报告整项作业完成。

正常任务不强制每台机器人都参加，也不强制所有请求都入水或编队。编队和跨介质只有被业务要求或在当前模型下有益时才应被选择；能力验收使用专门但仍真实的业务场景。不能用任务末尾附加一次编队或硬指定 AAV 入水来冒充联合选择。

如果模型既没有编队带来的真实观测/通信/运动收益，也没有业务要求，独立行动又不差，就不应期待工期优化器主动选择编队。不能为让演示出现编队而添加未经依据的奖励。

## 3. 能力、状态、角色、变量与约束的严格区分

| 对象 | 类别 | 例子与边界 |
|---|---|---|
| 传感器、可用通信设备、额定尺寸与工作域 | 基础能力/参数 | 有相机、声学设备、允许水下运行；必须对应实际装备或声明代理模型 |
| 已验证入水、出水、观测、编队等动作 | 操作能力 | 表示存在经过验证的动作方法，不代表任何状态都能调用 |
| 位置、速度、姿态、介质模式、故障、缓存 | 动态状态 | 由实际状态或合法预测提供，不由优化器任意设定 |
| 速度、姿态、位置属于入水允许集合 | 状态相关能力的前置条件 | 当前是否可入水是规则对状态的评价结果 |
| 观测者、支援者、编队成员 | 角色 | 由任务方法产生的职责；“具备中继设备”是能力，“本次担当中继”是决策 |
| 选择 UUV 方法或 AAV 跨介质方法 | 方法决策 | 由求解器选择，不能固定在业务请求中 |
| 时刻、顺序、路线、入口、支援窗口 | 决策变量 | 在声明的合法候选范围中搜索 |
| 共享物理成员、调度前置关系 | 资源/时序约束 | 同一成员不能接受互相矛盾的运动参考 |
| 链路是否可用、数据是否来得及传完 | 关系/交付约束 | 取决于多个节点的状态与安排，不是单台机器人的固定属性 |
| 任务覆盖、有效观测、必要交付、返回 | 业务约束/终止条件 | 不是可以用较小工期抵消的惩罚项 |
| J | 目标函数 | 衡量完整方案优劣，不执行搜索 |
| 搜索、HiGHS、运动查询、修复操作 | 求解机制 | 在目标与约束下产生、检验和改进方案 |

能力和约束不是互斥分类：能力给出可执行的范围，约束要求所选动作留在该范围。基础能力及评价规则是模型参数；当前可用能力是状态的派生量；未来可用能力由候选动作和状态递推确定，不能直接视为任意常数或自由开关。

AAV 当前处于 AIR 不能直接排除水下方法。应判断它能否经“转场—入水—观测—退出”到达观测所需状态。反之，只有 WATER 标签而没有合法入水或退出动作，也不能接受完整水下方法。

传感器和工作域通常按角色绑定；只有物理上可累加的量才求和。一台机器人有相机、另一台速度快，不等于团队可以按后者速度执行前者的拍摄。

任务目标所在的空间域与执行平台的运动模式也要分开。水面目标可能由空中或水面平台观测，前提是相应方法能满足观测标准；水下作业不等于只能由UUV执行。不得仅从区域标签直接指定唯一平台类别。

## 4. 系统输入输出与数学对象

### 4.1 输入

用以下对象描述一次规划实例：

- `R`：当前已激活的业务要求、任务关系、观测标准、目标接收端与终止条件；
- `Theta`：平台基础能力、闭环模型、几何、通信设备及政策、已验证方法范围；
- `I_t`：截至 t 实际收到的时间戳状态、计划确认、动作结果、观测终结与接收消息；
- `K_t`：已确认承诺以及结果尚不确定、不能擅自释放的承诺范围；
- `D`：有限方法、路线/入口、等待选择、时间网格和时域的本次搜索定义。

没有业务 deadline 就不添加人为业务截止。在线 10 秒求解预算与机器人任务 deadline 是不同参数。

### 4.2 完整闭环状态

对平台 i 定义：

\[
\zeta_i=(x_i,m_i,\eta_i^{ctrl},\eta_i^{act},e_i,\ell_i).
\]

`x_i` 是运动状态，`m_i` 是介质模式，`eta_ctrl/eta_act` 是控制器与执行器内部状态，`e_i` 是有模型时的能源状态，`ell_i` 包括当前参考来源、代次、活动阶段和必要本地执行记忆。通信状态单独记为接收前缀 `q` 和记忆 `mu`，后者包括已产生产品、回执、信道时间及去重信息。

相同位置与模式不代表同一可继续执行状态。不能将正在转弯的 UUV 或刚入水的 AAV 替换成同位置的固定 trim 初态。

母船预测使用 `I_t`：

\[
\hat\zeta(t)=\mathcal E(I_t,\Theta,K_t).
\]

这不是增加一个独立的“信息决策层”，而是约束状态输入的来源。平台本地可用实时真值，中央规划不能通过共享内存、评测日志或无条件远端查询绕过通信限制。

工程上不要求每次传回全部内部变量；可以发送足以重建的快照、已确认执行段及有效误差范围。若既不能重建也没有适用证据，则该候选的闭环可行性仍是 UNKNOWN，不能伪造完整状态。

### 4.3 唯一计划与内部输出

\[
\Pi=(z,Y,\sigma,\chi),\qquad \sigma=(S,F).
\]

z 是方法选择，Y 是角色与成员分配，S/F 是活动预订区间，chi 是已评价的参考、路线、模式序列和等待行为。第一阶段固定传输政策 Gamma_0，逐跳传输量和接收状态是推演结果：

\[
(q,\delta)=\operatorname{Transport}_{\Gamma_0}(\Pi,I_t,\Theta).
\]

因此第一阶段不把自由流量分配写成尚未接入执行器的控制变量。规划器仍然联合优化通信支援的位置与时间，因为它们改变 Gamma_0 的可用链路与结果。

| 部分 | 输入 | 输出 |
|---|---|---|
| 请求解释/实例化 | R、任务模板、地图与方法库 | 作业、观测与交付要求、可选方法；不提前固定平台 |
| 联合候选搜索 | 上述实例、I_t、K_t、预算 | 方法、角色、顺序、时段与待验证候选 |
| 时序/通信子问题 | 固定的部分选择、已知边界与容量 | 时间安排、有效下界、冲突或未决状态 |
| 运动执行查询 | 完整入口预测、方法、时段、地图与其他承诺 | 状态、耗时、全区间轨迹/包络、终态与适用条件 |
| 完整计划校核 | 所有成员活动、待命、通信政策及候选证据 | 可接受/不可行/未知，适用实例与原因 |
| 任务执行器 | 同一份已接受计划 | 原 Action、启动/取消/修复操作及结果关联 |
| 平台执行 | 本地参考与实时本机状态 | 实际运动、产品和终结消息 |
| 中央接收与修复 | 实际到达消息 | 更新后的 I_t、K_t、任务事实与受影响后缀 |

## 5. 统一约束模型

### 5.1 业务作业、执行方法与活动不同

业务作业 j 有合法方法集合 H_j。方法是活动与角色的模板，例如“UUV 观测 + USV 支援”或“AAV 转场 + 入水 + 观测 + 出水 + 交付”。每种方法具有前置条件、活动关系、观测效果和必要角色。活动按物理成员实际占用资源。

\[
\sum_{h\in H_j} z_{jh}=1,\qquad z_{jh}\in\{0,1\}.
\tag{1}
\]

这个等式针对当前必做作业。条件复查尚未触发时不作为已知必做作业，触发后生成新实例。只有策略原本覆盖相应分支时才能继续引用原完成时限证书。

活动 v 的激活量为 a_v。对方法规定的每个角色槽 r：

\[
\sum_i y_{ivr}=a_v,\qquad y_{ivr}\in\{0,1\}.
\tag{2}
\]

多人成员需求通过多个角色槽或明确基数表达。必要的角色互斥另行施加。角色数是方法定义，不是所有业务作业的全局固定人数。

共享 USV 支援活动可以服务多个业务作业，但物理占用只按同一个实际活动计一次。模型不得把每项观测强制封装成独占 USV、从出发到交付全程锁定的互不共享黑箱。

### 5.2 基础匹配、当前条件和过程条件

\[
y_{ivr}=1\ \Rightarrow\ \operatorname{BaseMatch}(Q_i,r)=1,
\tag{3}
\]

\[
y_{ivr}=1\ \Rightarrow\ \phi_{ir}(\hat\zeta_i(S_v),\Theta)=1.
\tag{4}
\]

开始条件之外，还有整个作业期间必须成立的工作域、传感器与角色关系不变量。不能仅检查开始时可用，之后越出工作域仍算成功。

对于可累加能力 u，可以使用：

\[
\sum_i y_{iv}\,q_{iu}\ge r_{vu}.
\tag{5}
\]

非可累加能力不使用这个式子。通信邻近、轨迹冲突和共享信道约束不应简化成该式中的一个布尔标签。

### 5.3 闭环运动、模式与完整预订区间

\[
\dot x_i=f_{i,m_i}(x_i,u_i,w_i),\qquad u_i=\kappa_{i,m_i}(x_i,r_i,\eta_i^{ctrl}),
\tag{6}
\]

并同时推进控制器、执行器等必要内部状态。上层选择参考和方法，不把所有平台改成同一个控制器，也不让优化器任意生成超出控制能力的理想位移。

\[
\zeta_i^-\in G_i^{m\to m'},\qquad \zeta_i^+=R_i^{m\to m'}(\zeta_i^-).
\tag{7}
\]

转换映射只表达实际允许的状态变化或坐标表示，不制造瞬移和第二个状态源。

对名义确定执行查询：

\[
\hat\zeta_i(F_v)=\Phi_{iv}(\hat\zeta_i(S_v),\chi_v,F_v-S_v;\Theta).
\tag{8}
\]

Phi 覆盖整个预订区间，包括转场、提前完成后的合法等待、终端控制与网格取整尾段。若名义完成时长为 tau，预订时长取整到 ceil(tau/Delta)Delta，必须把原控制器继续推进到取整后的 F_v，而不是复制完成时刻的位置。

时间上界并不提供唯一轨迹。如果要保证不确定完成时长下的安全和通信，需要覆盖所有相应轨迹的状态包络与合法终端政策。没有这种证据时，只报告名义预测，不声称鲁棒保证。

顺序衔接使用完整状态；稳健版本需要前动作终态集合包含在后动作入口集合中。所有未分配或待命平台也具有合法运动/待机延续，并纳入计划时域内的联合安全校核。

### 5.4 资源与时序

对成员 i 的资源通道 c：

\[
\sum_v \rho_{ivc}(t)\,\mathbf1_{[S_v,F_v)}(t)\le C_{ic}.
\tag{9}
\]

运动、传感器和通信设备按实际可兼容性建模。边航行边中继可以合法，但同时接受两个冲突运动参考不合法。过渡段、等待段和返航同样占用资源。

作业前置关系：

\[
S_w\ge F_v.
\tag{10}
\]

数据前置关系：

\[
S_w\ge T_{d,r(w)}^{recv}.
\tag{11}
\]

只要求在后继真正需要数据的接收端 r(w) 收到，不把所有依赖一概替换为母船收到。支援先启动与支援完成是不同事件，不能写成相反的完成依赖形成环。

没有任务截止时不加 D_j；已有硬截止时使用该业务完成时刻（可能是交付而非运动结束）约束。编队集结应包含慢成员到达、其他成员合法等待以及解散后的实际状态。当前后端只能接受哪些成员组合与模板，就只在该范围中选择。

### 5.5 观测有效性与任务交付

第一版使用声明的几何观测代理；这不代表真实图像清晰度、识别精度或载荷质量已经验证。[R8]

对兴趣点 p，合法观测要求同一成员 i 在连续区间内满足对应工作域、视场和无遮挡条件：

\[
\exists i,[a,b]:\ b-a\ge d_p^{min},\quad \mathcal V_{ip}(\zeta_i(t),\Theta)=1\quad\forall t\in[a,b].
\tag{12}
\]

采用采样评测时，应标明这是采样条件并约束最大采样间隔，而不是将样点通过直接升级为连续时间结论。多个成员的短驻留不能默认拼接为同一连续驻留。

生成产品必须由合法观测/终结事件触发；产品携带任务、目标、尝试、产生时刻和版本。重复传输不能生成新产品。复查可生成同一目标的新产品，但目标覆盖结算要去重并采用业务要求的有效版本。

定义 c_p^del 为存在合格观测产品且已被指定接收端接收，则：

\[
\frac{\sum_p w_p c_p^{del}}{\sum_p w_p}\ge C_{req}.
\tag{13}
\]

运动结束、观测终结报告、必要产品接收、物理资源承诺结束是四个不同事件。缺测终结报告可以传递且触发复查，但不计入成功覆盖。未收到报告只表示未知。

### 5.6 复制式去重前缀通信模型

每个消息/产品 d 有源 s_d、目的 r_d、产生时刻 g_d、大小 B_d 与类型。业务数据、观测终结、状态、命令、接纳和取消确认都按各自定义参与传输。母船只对以它为目的的业务产品是吸收端，它必须有下行发送能力。

q_{v,d,k} 表示节点 v 在步首已具有的连续有效前缀，满足 0 <= q <= B_d。源只有合法产生后才具有 B_d；未产生产品不能发送。

冻结一套可执行政策 Gamma_0，声明各信道、有效流顺序和链路判断。所有发送方可用前缀读取同一步首快照；接收方去重读取本步已经累计的前缀。

对信道处理序列中的第 ell 个流 (u,v,d)，设可用条件 chi_ell 为 0/1，信道剩余预算为 C_ell，接收方当前前缀为 tilde q：

\[
\delta_\ell=\chi_\ell\min\left\{C_\ell,\,[q_{u,d,k}-\widetilde q_{v,d}^{(\ell)}]_+\right\},
\tag{14}
\]

\[
\widetilde q_{v,d}^{(\ell+1)}=\widetilde q_{v,d}^{(\ell)}+\delta_\ell,\qquad C_{\ell+1}=C_\ell-\delta_\ell.
\tag{15}
\]

一轮所有流处理完后写回 q_{k+1}。不同信道的发送方仍读取同一步首快照，所以新收到的数据不能在同一步内立刻跨信道转发。相同目的端从多个来源接收时用更新后的 tilde q 去重，不能将各个来源相对步首目的前缀的差值直接相加。

每个声明信道满足：

\[
\sum_{\ell\in c}\delta_\ell\le C_{c,k}.
\tag{16}
\]

当前常速简化模型的预算为 rate_c * elapsed_time；链路变化应按模型规则切分区间。不同设备可以并行；同一个半双工设备的链路共享资源，不能把声学与 RF 无依据地全合并或全独立。实际模拟器目前用有限计数器而非真实文件传输，模型边界必须保留。[R7]

本次固定政策的整体写法是：

\[
(q_{k+1},\mu_{k+1})=\mathcal T_{\Gamma_0}(q_k,\mu_k,\chi_k(\hat\zeta,\Pi),\text{本步有效产生事件}).
\tag{17}
\]

这里的流量不是独立自由变量。HiGHS 若只加容量不等式并自由分配流量，得到的是另一种政策或一个需要证明包含关系的松弛，不是 Gamma_0 的精确实现。精确参考必须重现同一政策；后续要优化逐跳分配，必须由执行器真正消费该分配。

产品交付要求 q_{r_d,d}=B_d，且必要元数据/终结消息按业务规定已接收。相同产品只结算一次。保留副本会增加各节点保存量之和，不违反信息因果。

设备缓存是另一件事。q 是信息前缀，不自动等于真实内存使用量。没有原始数据、副本、编码、元数据与删除机制的可靠占用模型，不声称缓存容量已证明可行。引入可靠缓存模型后，再加实际缓存占用不超过设备容量的约束，不重写当前前缀语义。

### 5.7 运动与承诺安全

名义轨迹至少校核工作域、障碍和平台间距离：

\[
\operatorname{dist}(p_i(t),\mathcal O)\ge r_i+d_o,\qquad
\|p_i(t)-p_j(t)\|\ge r_i+r_j+d_{safe}.
\tag{18}
\]

上述用球形几何代理时必须标明代理范围；若船体形状不宜用球体，使用对应实体集合的距离。仅采样校核不等于连续安全。

计划下发、接纳、取消和结果更新按版本和活动身份对应。已发出但接纳情况未知的动作不能被当成未发生；未确认取消不能释放旧承诺。安全接纳应兼容所有尚可能执行的承诺，而不是只兼容中央希望生效的新版本。

规划耗时期间世界继续推进。计算输出需在实际采用时刻重新检查入口状态、有效版本和承诺，或预先规划到未来生效时刻；不能把 10 秒前快照当作刚好仍在原地的真值。

### 5.8 能源、返回与终止

能源只有存在可信闭环功耗模型时才递推并约束，例如 e_i(T) >= e_i^reserve。已经在动作链中计入返回能耗后，不再重复扣一次返回预算。无可信模型时将其列为未验证边界，不能将“无限电量假设”包装成能量可行性证明。

最终结束条件包含当前必做任务的有效结果、必要数据与关闭报告接收、规定返回/退出，以及要求释放的资源承诺终结。不是任意一个 Action SUCCEEDED 就完成全任务。

## 6. 统一目标函数与有限问题

固定一次实例的候选、参数、时间范围、初始信息和通信政策，定义名义有限问题：

\[
\boxed{P_{\Delta,\Gamma_0}:\quad
\min_{\Pi}\ J_t(\Pi)=T_{end}(\Pi)-t
\quad\text{s.t.}\quad
\Pi\in\mathcal F_{\Delta,\Gamma_0}(R,\Theta,I_t,K_t).}
\tag{19}
\]

T_end 是所有要求的终止条件成立的时间，不只是最后运动结束。第一版不堆任意能量、迟到、安全权重。安全和必做要求是硬约束。若允许部分完成或迟到，需显式定义另一业务政策，不能自行降低要求。

该问题是离散方法、时序和闭环执行关系耦合的规划问题，未必是单个 MILP。有限离散化和固定候选之后，部分时序/通信关系可交给 HiGHS；完整动态候选仍须通过原生查询与整体校核。

只有已验证的候选和连续物理合同构成保守可行集合时，才可声称其可行域包含于物理可行域。仅有名义轨迹与样点检查时，不自动具有该包含关系，更不能继承相应物理最优值不等式。

复查新增、候选库扩大、地图/状态改变或通信政策改变后，是新的 P 实例。旧可行解要重验，旧全局 gap 要重新建立。

## 7. 联合求解方法

### 7.1 多模块联合的数学含义

将决策分为 d 与 y：d 是方法、成员等部分离散选择；y 包含尚未固定的时序、路线和其他决策。固定 d 定义：

\[
V(d)=\inf_{y:(d,y)\in\mathcal F}J(d,y),\qquad \inf\emptyset=+\infty.
\tag{20}
\]

于是：

\[
\inf_{(d,y)\in\mathcal F}J(d,y)=\inf_d V(d).
\tag{21}
\]

证明：原可行域是各个固定 d 的可行分片之并，对同一集合分组后取下确界不改变值。有限离散范围和闭合有界子问题下，最小值可取得。

这不意味着任意交替局部优化都能达到右侧的 V(d)。如果子问题尚未精确解决，应保留区间/未决分支，不能将一个局部解当成子问题全局最优。

### 7.2 第一阶段的实际算法

同一 `build_executor_plan` 联合入口中，方法/角色/等待/时段候选生成、按需运动查询、通信重放和完整计划校核交错运行。先找完整可行解，再在剩余预算内改进。旧 v9 评分可以仅作为不改变可行域的搜索排序参考，但不得决定新问题的最终接受标准或产生自动回退。

当前完整候选代码存在，但它是带预算枚举，不是已完成的带全局上下界证书分支定界。[R5] 尤其现在主要按最早可用时刻展开；若通信可行性需要故意等待，必须将合法等待或可选开始时段纳入搜索，否则会漏掉可行方法。

### 7.3 第二阶段的有界搜索

节点 N 表示该部分方案所有尚可能合法的补全 Omega(N)。子节点必须覆盖父节点全部补全：

\[
\Omega(N)=\bigcup_{M\in child(N)}\Omega(M).
\tag{22}
\]

固定全体声明候选，而不是只定义目前已经算完的候选。未生成、超时和 UNKNOWN 候选可由父节点或未决任务代表，不能从边界消失。

构造包含原补全的松弛 R(N)，使用 HiGHS 得到有效下界：

\[
\Omega(N)\subseteq\mathcal R(N),\quad
L(N)\le\inf_{\Pi\in\Omega(N)}J(\Pi).
\tag{23}
\]

可以从零下界开始，再放松资源互斥、采用经证明的最短耗时/最高速率界或整数松弛加强。仿真成功轨迹的耗时通常是某个可行方案的代价，不自动是最短耗时下界。HiGHS 的可行解值与数值下界必须区分。[R3]

完整校核的可行方案提供 U。仅在确定无合法补全，或 L(N) >= U 时剪枝。局部运动优化失败不证明所有路径失败；未增加观测 trait 不代表通信支援没有价值。

所有模块共享一次在线单调预算。HiGHS 内部 time_limit 之外仍要处理进程/计算边界的中断与退出，不能宣称单个参数就是整个系统严格实时证明。离线精确参考可以有不同预算，但必须采用同一实例。

### 7.4 接受条件

```
任务/方法/成员/时段候选
        ↓
能力与时序的必要条件筛选
        ↓
HiGHS时序子问题/下界（具备对应模型时）
        ↓
Swarm/qn/PVS全状态查询
        ↓
同一Gamma_0的有限交付重放
        ↓
全部物理成员、承诺与终止条件校核
        ↓
完整可行 → 更新U
需要改变时序/角色/路线 → 回到同一搜索
UNKNOWN → 保留未决身份和范围
```

运动改变后必须重评链路；开始时刻改变后必须重推合法等待和入口状态。后端执行中采用新参考时，必须证明仍处于原允许集合或触发重验/处置。只验证一条预测轨迹不能授权任意后续原生重规划。

## 8. 可直接使用的理论命题及边界

### 8.1 信息因果与去重

假设产品只能合法产生，初始前缀在 [0,B_d] 内，且传输按式(14)–(15)执行。

每次增量非负，并且 tilde q_v + delta <= max(tilde q_v, q_u,k) <= B_d，所以前缀单调且有界。若所有源都未产生产品，则全体前缀为0，传输增量恒为0。发送方读取步首前缀，禁止同一步跨多跳使用新数据；接收方读取本步累计前缀，避免不同上游的重复计数。按时间步归纳，信息不凭空出现，目的端同一前缀不重复结算。

这不证明所有节点保存量之和守恒。8字节产品经过源—中继—母船复制，保存前缀之和可为8、16、24，唯一交付量始终不超过8。缓存物理可行性需独立模型。

### 8.2 有限模型内的计划可行

若完整候选满足式(1)–(18)，且查询/重放使用同一固定模型、完整状态和政策，则该候选属于所定义的有限名义可行域。这个结论证明模型内部一致性，不证明未建模扰动下的真实物理安全。

如果另外有有效动作入口—过程—终态合同、全区间联合碰撞证据及通信服务下界，可沿动作与接收事件归纳，将计划可行性传递为这些合同范围内的执行可行性。无合同则不能越过这一层。

### 8.3 最优性区间

设 U 为完整可行解代价，F 为全部尚未解决边界，包括 UNKNOWN 和未生成但已声明的候选。

\[
L_g=\min\{U,\min_{N\in\mathfrak F}L(N)\}.
\tag{24}
\]

边界为空时令 L_g=U。在分支覆盖完整、下界有效和剪枝正确下：

\[
L_g\le J^*_{\Delta,\Gamma_0}\le U,\qquad
0\le U-J^*_{\Delta,\Gamma_0}\le U-L_g.
\tag{25}
\]

证明：U来自可行解；任何更优解仍由某未决节点表示，其有效下界不超过该解代价；合法剪枝不会丢弃更优解。故上下夹逼成立。

有限候选、有限时域且必要子问题可在有限时间内明确解决时，公平遍历与有效剪枝能有限步得到有限模型最优或证明该有限模型无解。UNKNOWN不能消解时，只能报告当前区间和未决范围。子问题gap不等于整个搜索gap。浮点容差需单独记录，显示gap为0不能自动写成无条件精确算术证明。

### 8.4 保持承诺的修复一致性

新事件改变 I_t 后，识别任务、动作状态、共享资源、运动交互、数据和信道的受影响约束。冻结已完成事实、不可中断已接受片段和仍有效的未来会合。重搜未承诺部分，重验所有跨修改区与冻结区约束。

若未重验约束的全部输入不变且原来成立，所有变化或跨边界约束均重新成立，则修复计划满足全约束。证明是按约束集合分类逐项成立，不要求把全部计划从头求解。

想减少查询，必须记录真正的依赖输入（完整状态、参考版本、时间段、地图、伙伴参考、通信政策等）。确定性查询的全部依赖不变，才可复用结果。变化影响很广时，可能仍需大量重验，不能保证每次都严格更少。

局部搜索失败应扩大到全部未承诺部分。冻结额外未承诺决策得到的局部最优，不等于所有未承诺部分的全局最优。信息或能力改善后，还需恢复原先受影响的剪枝分支，不只重验旧路径。

### 8.5 条件性递归可行，不是默认保证

旧剩余计划只有在新可能状态仍处于其合同范围、未来通信服务仍满足其假设、必做任务没有增加、承诺仍有效、剩余计划仍被新候选域表示时，才可作为新问题可行解。

证明：旧策略原本覆盖新状态集合，所需未来条件未变化，其剩余部分仍满足后继约束且在新搜索域中，所以新问题至少有该可行解。

任务新增、缺测复查新实例、永久故障、错过不可恢复通信窗口，均可能破坏条件。旧计划失效后不能因为曾经是incumbent而继续派发。无有效新计划时，由已验证本地处置接管并报告任务状态，不假装仍然可完成。

### 8.6 安全与信息年龄的条件性推导

若有效跟踪/预测界给出 ||p_i-p_i_ref|| <= epsilon_i，参考中心距离至少为 r_i+r_j+d_safe+epsilon_i+epsilon_j，则由三角不等式推出实际表面净距至少d_safe。

如果远端预测误差满足 dot epsilon <= L epsilon + w_bar，则 L>0 时：

\[
\epsilon(\tau)\le \epsilon_0e^{L\tau}+\frac{\bar w}{L}(e^{L\tau}-1).
\]

允许误差delta >= epsilon_0 时，充分条件是：

\[
\tau\le\frac1L\ln\frac{\delta+\bar w/L}{\epsilon_0+\bar w/L}.
\]

这是模型已被证明成立时的信息年龄限制，tau按消息生成时刻计算。收到了旧消息不等于状态变新。该条件可以生成通信接收期限并反向影响任务方法/支援安排，但当前未验证的L、扰动界和转换误差不能补造数值。AAV编队内部轨迹广播也应有明确通信假设。

## 9. 代码事实与必须调整的地方

此次只读核查的代码事实如下。[R4–R9]

| 位置 | 当前可确认事实 | 建议调整 |
|---|---|---|
| `models.py` | 能力标签、任务前置、固定成员数、ExecutionCandidate及终态已存在 | 业务层不强制平台/方法人数；所需角色与人数移到具体方法语义；保留真正业务硬要求 |
| `executors.py::eligible_executors` | 先按标签子集和人数筛选 | 不因当前AIR排除合法跨介质方法；区分作业角色与通信支援角色 |
| `_build_complete_candidate_plan` | 带预算枚举、协作活动、完整计划校核；UNKNOWN主要记布尔标志 | 增加等待/时段选择、持久未决分支、有效下界；不是仅换函数名 |
| `monitoring_request.py` | 存在历史AIR展开入口、几何覆盖代理与部分旧拒绝逻辑 | 统一完整业务请求到当前方法生成；不能把旧入口局限推断成所有当前路径都不支持水下 |
| `observation_coverage.py::FiniteDelivery` | 复制式前缀、固定流顺序、共享步首发送快照 | 保持预测/执行一致；通用消息类别与真实下行路径仍需接通，不能沿用只向mother交付的路由器冒充通用网络 |
| 原生查询/worker | 部分完整模型状态和原生片段已接入，完整三类链仍有缺口 | 接通AIR、跨介质、终端/待命的全状态查询，验证参考更新 |
| 修复/接纳 | 有结果与时间修复基础 | 重搜受影响未承诺部分；跨边界重验；旧结果不能释放新承诺 |

`context/02_current_status.md` 的顶部仍明确列出完整AAV查询、模式选择、返回复查及受限状态消息的未完成项。不能因模型在文稿中出现，就标成当前代码已完成。

## 10. 实施顺序与验收

第一阶段统一任务、方法和产品语义，贯通同一请求。先固定传输政策与名义模型，不引入第二求解服务，也不把业务决策变成用户复选框。

第二阶段使全状态运动查询覆盖每个实际成员的整个规划区间，接通完整AIR/WATER方法、支援和规定返回。动作时间变化后重推等待和通信，不修改原动力学来凑预测结果。

第三阶段完成一份已接受计划的实际执行、实际接收和保持承诺修复。计划确认一次不等于中途永不更新；在最初授权范围内，内部修复依版本和节点接纳机制执行。

第四阶段在同一入口加入HiGHS参考模型、下界、可恢复边界和选择性查询复用。端到端闭环不以必须先实现严格gap为前置，但没有证书时也不宣称最优。

关键验证集中在：

1. 同一位置不同控制/执行器状态的可行性差异，跨介质前后状态连续；
2. 同源副本、逐跳转发、重复接收和多个上游的去重，下行命令与数据共享声明资源；
3. 单独可行而组合碰撞/容量不足，支援增加可以降低总工期；
4. 最优候选UNKNOWN时不能报告零gap，单路线失败不能删掉其他方法，必要等待不能漏搜；
5. 旧结果、旧取消、断联未确认不得解锁新承诺；通信窗口错失后实际采用新的未承诺安排；
6. 同一任务完成空中/水下作业、必要交付、至多一轮复查及返回；普通请求允许某能力不被选中，另做同场景能力验收。

实验应分开报告作业完成、实际接收、安全采样、资源状态、证据范围、规划和修复耗时；有边界证书后再增加L_g/U/gap。对照使用固定历史版本而不是生产自动回退。禁止把未知项换成“无限电量”“理想通信”“固定trim状态”后仍声称同一问题通过。

## 11. 研究贡献与正确性结论

目前可以给出的判断是：按上述定义，研究问题与预期闭环一致，能力/角色/状态/约束没有混用，复制式通信与现有实现语义相容；一般分解、下界、有限最优和修复命题具有明确适用前提。

还不能宣称：当前全部代码符合本文、所有方法具有物理保证、任意断联或永久故障下任务最终完成、10秒内必然证明连续全局最优。

拟贡献应落在两个可验证结果：一是状态相关方法与必要交付如何共同改变平台、支援、编队和时序选择；二是在完整闭环状态和执行承诺下，怎样通过正确依赖识别、有效剪枝与查询复用降低修复计算量而不漏检跨边界约束。通用分解、分支定界、三角不等式与“用了能力标签”本身不是原创统一控制理论。

最终方案不是“重新发明一个整体框架”，也不是“继续套用Calvo v9”。它是在现有Heformation工程里，让同一个联合问题、同一套候选评价、同一个计划及同一执行语义贯穿任务输入—状态输入—求解—执行—接收—修复全过程。

## 12. 核查来源

[R1] Calvo & Capitán, *Heterogeneous Multi-robot Task Allocation for Long-Endurance Missions in Dynamic Scenarios*, arXiv:2411.02062v3，本文读取HTML。https://arxiv.org/html/2411.02062v3

[R2] Neville et al., *D-ITAGS: A Dynamic Interleaved Approach to Resilient Task Allocation, Scheduling, and Motion Planning*, arXiv:2209.13092，本文核对摘要和论文记录，未重新宣称阅读全文。https://arxiv.org/abs/2209.13092

[R3] HiGHS官方：LP/MIP/QP数值优化及接口说明。https://highs.dev/

[R4] 当前代码版本：8274cd73039cdc37e9f0980f785936894d3de508。https://github.com/CquL/Heformation/commit/8274cd73039cdc37e9f0980f785936894d3de508

[R5] 联合候选与原v9分支，`integration/mrta_python/executors.py`，此次读取源码922–985与1095–1272行。https://github.com/CquL/Heformation/blob/8274cd73039cdc37e9f0980f785936894d3de508/integration/mrta_python/executors.py

[R6] 任务与候选对象，`integration/mrta_python/models.py`，此次读取1–165行。https://github.com/CquL/Heformation/blob/8274cd73039cdc37e9f0980f785936894d3de508/integration/mrta_python/models.py

[R7] 复制式传输和声明链路，`observation_coverage.py`，此次读取14–148行。https://github.com/CquL/Heformation/blob/8274cd73039cdc37e9f0980f785936894d3de508/integration/qn_aav_simulator/src/qn_aav_simulator/observation_coverage.py

[R8] 任务展开与几何代理，`monitoring_request.py`，此次读取1–184行。https://github.com/CquL/Heformation/blob/8274cd73039cdc37e9f0980f785936894d3de508/integration/qn_aav_simulator/src/qn_aav_simulator/monitoring_request.py

[R9] 当前与历史阶段边界，`context/02_current_status.md`，此次读取顶部1–27行。https://github.com/CquL/Heformation/blob/8274cd73039cdc37e9f0980f785936894d3de508/context/02_current_status.md
