# 项目指令入口

项目说明正文在根目录[AGENT.md](AGENT.md)。开始工作前实际读取该文件，再按其最小阅读集加载当前状态与交接。

本文件只作为使用`AGENTS.md`入口的工具的薄引导，不维护第二套项目规则，也不把`prompt/`中的提示词自动当作本次执行授权。

当前采用背景包2.0：不继承旧HUC；先独立复现上游，以原生接口和最少适配组成完整协调闭环，再依据实验问题研究创新。
