# 新会话：按V2接续

> 背景包2.0任务正文。先读项目AGENT；操作范围由本次用户授权决定。


阅读AGENT.md、content/02_current_status.md和content/15_handoff.md；再只补读本次工作相关的文档。旧HUC不再是依据，不访问、审计或引用其实现，也不从旧记忆恢复它的P0和接口规则。

先辨明当前属于上游复现、选型、原生接口集成、实验还是研究。用户确认的Swarm复现作为起点；新增成果没有运行证据就保持待复现。第一版目标仍是完整母船联合规划协调器与节点执行闭环。

只使用本次指定或新工作区中的工程和被选上游。若当前目录仍指向被排除旧项目，不继续读取其实现；说明需要切换到新工作区/指定上游，但不把此事改成旧项目审计。

用简短文字说明当前目标、实际证据、下一项可执行动作，然后按本次授权推进。不要重画八层系统，不自动安装全部候选，不先写新的TaskContract/Envelope/Certificate。没有要求查新时，不把整个文献库重新搜索作为开始工作前提。

输出本次实际完成与未完成。结束时按09更新状态和交接，不将资料阅读记为实验通过。


返回：[提示词索引](README.md)。
