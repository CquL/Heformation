# 独立复现一个上游原始例子

> 背景包2.0任务正文。先读项目AGENT；操作范围由本次用户授权决定。


先读AGENT.md、content/07_reproduction_integration.md及本次选定上游的来源条目。复现目标以用户当前指定为准；未指定时使用01确定的最小目标。禁止把旧HUC作为运行外壳或前置审计对象。

确认所选上游的实际路径、commit、未提交改动、README例子、依赖和输入数据。只收集与本次原例有关的环境信息，不输出凭据、许可内容或全部环境变量。原始代码和结果保留，实验在隔离工作区执行。

按作者原始模型、求解器、配置和入口运行。不要同时做ROS迁移、算法改动、动力学替换和topic重命名。必要的构建适配要单独记录，不能把移植或重新实现伪称为原代码复现。

先完成小例子并验证输入到输出的实际链路：输入来自哪里、状态由谁产生、任务/轨迹如何输出、完成如何报告。Primitive需明确是导航不是严格编队；调度例需检验资源/依赖语义而不只看甘特图。

若失败，优先定位确切依赖或参数问题，保护原文件，不通过关闭安全/更改任务来制造成功。没有许可等条件时说明阻塞，不无限循环安装，不静默替换求解器。

交付实际命令、版本、配置、日志位置、观察结果和未验证范围，一份短记录足够。仅真实运行后才更新02/15的复现状态。


返回：[提示词索引](README.md)。
