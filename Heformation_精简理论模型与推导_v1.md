# 异构无人集群任务—运动—有限交付联合规划
## 精简理论模型与推导 v1

日期：2026-09-21。

本文是一份建模与数学推导稿，不是当前仓库全部实现、实跑通过或原创性已验证的声明。需求基线为本轮用户提供的设计材料：用户指定观测与结果要求，系统选择平台、模式、路线、支援和时序；不强制固定入水点、固定编队、USV中继或任意业务期限。该材料中的旧版本代码审查不用于判断当前代码状态。

---

## 1. 收敛原则

上一版“方法执行约束摘要”六元组不是新的物理定律，而是对初终态、轨迹、活动时段和观测条件的再次包装。v1 不把它单列为理论贡献或新增软件实体。运动评价只需返回：是否可行、各活动耗时、完整终态，以及用于观测/安全/链路评价的运动轨迹；状态和轨迹保留在已有查询内部。

| 内容 | v1处理 |
|---|---|
| 方法执行约束摘要/通用合同管理 | 不单独建立，复用现有执行方法及运动查询 |
| 任务、模式、物理成员、时序 | 保留，它们决定分工与资源竞争 |
| 位置、速度、姿态和后端内部状态 | 保留其物理作用，不全部作为上层独立决策变量 |
| 障碍、海底、禁区、转换资格 | 归入运动可行性，不增加“环境奖励” |
| 距离、RSSI、丢包、干扰、码率等完整网络参数 | 第一版用设备/介质可用性、有效服务率和必要延迟汇总；底层可用这些因素估计服务率 |
| 有限产品量、逐跳因果、共享容量、接收前置 | 保留，不能以连通代替交付 |
| 未标定能量目标、波浪耦合、任意AoI权重 | 暂不纳入主模型；需要时依据任务和可靠模型增加 |
| 完整POMDP、概率碰撞、全局可达集 | 暂不纳入；采用可用信息下的名义预测与事件更新 |
| 任意联盟、任意队形、无限模式序列 | 仅生成当前任务需要且平台支持的方法，不默认搜索全部可能组合 |
| GoalID、接收身份、状态时间戳、故障占用 | 保留为执行正确性机制，不当成连续优化变量 |

压缩符号本身不降低计算量。真正降低计算量的是减少没有业务作用的候选分支、避免重复运动查询，并在不丢失更优可行方案的前提下使用有效下界。不能仅凭某条轨迹失败就剪去整个执行平台或方法。

---

## 2. 建模范围和假设

### 2.1 平台与任务

物理平台集合为

\[
\mathcal I=\{A_1,A_2,A_3,S,U\},
\]

分别对应三台两栖 AAV、一台 USV 和一台 UUV。母船记为节点 0，作为固定逻辑指挥/接收端，不增加一个待优化的移动机器人。

每轮规划处理当前已知且必须完成的有限任务集合 \(\mathcal J_t\)。未知对象或补查任务仅在有效观测/接收事件激活后加入下一轮，不假设提前知道未知结果。首版任务由明确模板展开，不需要通用自然语言规划、任意时序逻辑或全场景任务生成系统。

### 2.2 名义规划与真实执行的边界

本轮依据已经收到的状态和环境信息构造名义预测，运动、观测与通信条件均有明确来源。理论首先讨论所声明名义模型中的可行性和解质量。实际状态偏差或环境更新使预测条件失效时，重新评价可修改部分；不把名义结果升级为任意扰动下的连续安全或递归可行性。

断联不是允许读取仿真真值。缺少足以支持新决定的信息时，不新增未经支持的承诺。平台可以在已授权片段内依靠本地信息执行或进行合格的安全处置。

### 2.3 通信简化

每个产品在一次被接受的交付片段中使用一条无环路由，允许逐块转发，不要求中继先收完整个产品。第一版使用共享时分信道和有效速率模型。若实际设备支持边运动边通信，该行为允许；若设备为半双工，则加入该设备对应的同一时间资源限制。

产品、必要计划/确认消息均消耗其声明的通信服务。高频邻机轨迹通信可采用已验证的单独服务假设，但不能完全省略并声称所有通信均受同一有限链路约束。

缓存第一版可不作为优化瓶颈，但必须确认一次任务的最大同时保留量不超过可用缓存；否则必须加入缓存上限。这个简化不等于无限缓存，也不允许发送后未经确认删除数据。

---

## 3. 最小输入、状态与输出

### 3.1 用户输入

任务区域/对象、有效观测标准、规定接收方、结果触发的后继规则、真实存在的外部限制与返回要求。用户不填写执行者、入水点、中继出发时刻或固定编队流程。

### 3.2 任务层状态

\[
S_t=(\widehat X_t,\sigma_t,b_t,\mathcal C_t).
\]

- \(\widehat X_t\)：有时间戳的已知/估计平台状态。每个平台的模式单独记录，完整运动状态留在后端预测中。
- \(\sigma_t\)：任务实际进度，包括未开始、进行中、观测满足、失败等。
- \(b_t\)：已知的数据产生/接收进度。未知远端进度不能写成已知收到。
- \(\mathcal C_t\)：已接受活动、物理占用和未来会合等承诺。

环境预测 \(\widehat{\mathcal M}_t\)、平台模型 \(\Theta\)、链路服务预测 \(C\) 是本轮计算条件，不全部展开为上层自由变量。不要重复存一套“任务状态”和一套相互竞争的“计划真相”。

### 3.3 方法与活动

每个任务 \(j\) 有合格候选方法集合 \(\mathcal M_j\)。方法包含执行成员、模式序列、内部活动及依赖，具体入出水位置与路线是其几何参数，不是用户固定输入。

同一水下任务可比较：UUV+USV、AAV出水直接交付、具备相应设备资格的AAV+USV等。是否生成某种方法由能力、观测要求和动作资格决定，不由机型名称推断。

方法内部活动 \(a\) 记录执行成员 \(\mathcal I_a\)、开始/结束时刻 \(s_a,f_a\)、动作参考及启动/完成条件。USV转场与UUV观测是两个可并行活动，不将其耗时串行相加。

### 3.4 最小运动查询

\[
\operatorname{Eval}(a,x_i(s_a),\widehat{\mathcal M}_t)
\longrightarrow
(\text{status},d_a,x_i^+,p_i(\cdot)).
\]

这是已有计算的数学写法，不要求增加新接口层。多成员活动返回每个成员的终态与轨迹。观测产生时刻由实际/预测轨迹与既定观测规则计算，不需要另建产品注册中心。仅返回一个聚合“方法总耗时”不足以安排多平台并行活动。

---

## 4. 决策变量：只保留四组

\[
\Pi=(y,s,\vartheta,u).
\]

- \(y=(z,\sigma)\)：离散选择组。\(z_{jm}\in\{0,1\}\)选择任务方法，因此同时决定执行平台、模式结构、支援关系和编队方法；\(\sigma\)选择必要的资源/运动冲突顺序。
- \(s\)：连续活动时刻。结束时刻由开始及预测耗时得到。
- \(\vartheta\)：路径/轨迹参数、自动选择的入出水和会合几何。离散候选与连续细化可组合使用。
- \(u\)：各传输跳在各时隙发送的有效字节量。

平台分配可由 \(z\) 推出，模式由选定方法与当前步骤推出，角色由活动语义推出。不要再将同一个决定分别用几套变量表示。

位置轨迹不是凭空自由选择的状态：其结果由候选参考、实际初态和平台模型决定。字节累计量也是 \(u\) 的累计结果，不是可以任意赋值的独立量。

---

## 5. 统一优化问题

### 5.1 主要目标

令 \(\mathcal K\) 为必要产品集合，\(\mathcal I_R\) 为有规定终端/返回的平台集合：

\[
\begin{aligned}
\min_{\Pi,T}\quad&T\\
\text{s.t.}\quad
T&\ge f_j^{\mathrm{work}},\quad j\in\mathcal J_t,\\
T&\ge r_k^{\mathrm{receipt}},\quad k\in\mathcal K,\\
T&\ge f_i^{\mathrm{return}},\quad i\in\mathcal I_R.
\end{aligned}
\tag{1}
\]

每个活动必要的合格结束行为要包含在相应完成时刻中。不存在的返回/交付要求不增加。业务有硬截止时，才增加相应上界；求解预算不是业务截止。

### 5.2 约束A：任务与方法

\[
\sum_{m\in\mathcal M_j}z_{jm}=1,\qquad j\in\mathcal J_t.
\tag{2}
\]

所选方法须满足该任务全部规定观测条件。第一版允许一个方法内部包含多个观测活动，不要求开发跨任意方法的通用集合覆盖求解器。观测能力不同于平台数量：缺少某种传感器不能靠增加几个不具备它的机器人补足。

### 5.3 约束B：时序、状态和物理资源

\[
f_a=s_a+d_a.
\tag{3}
\]

\(d_a\) 来自完整执行预测，包含该活动应计入的转场、作业和终端行为，不能再重复添加一次转场费用。

对真正的完成前置和接收前置：

\[
s_b\ge f_a,\qquad s_b\ge r_k^{\mathrm{receipt}}.
\tag{4}
\]

对同一物理成员的互斥活动：

\[
f_a\le s_b\quad\lor\quad f_b\le s_a.
\tag{5}
\]

不同执行端点若对应同一台AAV，仍按式(5)互斥。通信设备与运动控制资源分开，允许能力支持的边航行边传输。

连续活动的初态采用上一活动真实/预测终态。中间存在空档时，以合格等待/滑行过程推进，不能把速度和姿态清零或假定欠驱动平台悬停。所有平台在整个评价区间内都有实际可解释的运动过程，包括未分配任务的待命成员。

前置区分启动确认与完成确认。USV先接受并启动支援，不意味着UUV必须等待USV全部运动完成。

### 5.4 约束C：运动、模式、环境与观测

保留已有各平台闭环：

\[
\dot x_i=F_i^{\mu_i}\bigl(x_i,\kappa_i^{\mu_i}(x_i,\rho_i),w_i\bigr).
\tag{6}
\]

本版名义预测使用声明的 \(w_i\)，不额外求鲁棒可达集。完整控制器、执行器与动力学参数从后端读取，不重写一套统一推力控制律。

所有选定运动及等待过程必须满足：合法初终态与模式转换、环境无碰撞、平台间安全间距、任务观测条件和所需本地信息条件。把这些写成

\[
\operatorname{MotionObsFeasible}(\Pi;S_t,\widehat{\mathcal M}_t,\Theta)=\mathrm{true}
\tag{7}
\]

只是紧凑记法，不能将细则藏起来。其检查内容至少为：

\[
\operatorname{dist}(\mathcal B_i(x_i(\tau)),\mathcal O(\tau))\ge d_{\rm safe},
\quad
\operatorname{dist}(\mathcal B_i(x_i(\tau)),\mathcal B_l(x_l(\tau)))\ge d_{\rm safe},
\tag{8}
\]

以及符合该任务模板的观测谓词。连续驻留不能用互不相连的短驻留任意相加。

入/出水位置为从当前可行水面自动选出的参数；转换段全部检查，不仅检查水面上的一个点。没有外部规定时，不添加入水点等于出水点或完成每个样点都必须返航的约束。

Swarm仅用于其适用的AIR独立/编队运动。原编队图与连续轨迹优化保留；上层评价集结、等待、编队运动和解散之后的完整影响。几何相似度不是业务收益，不设额外编队奖励。

### 5.5 约束D：有限通信

固定选定产品路由后，第 \(h\) 跳使用链路 \(e_{kh}\)。时间网格为 \(t_n=t_0+n\Delta\)。定义 \(u_{kh}[n]\ge0\) 为时隙 \([t_n,t_{n+1})\) 发送的有效字节量。

累计发送量由它直接给出：

\[
F_{kh}[n+1]=F_{kh}[n]+u_{kh}[n].
\tag{9}
\]

设 \(d_{kh}\ge0\) 是完整发送时隙以外的接收延迟步数：

\[
A_{kh}[n]=F_{kh}[n-d_{kh}],\qquad A_{k0}[n]=G_k[n].
\tag{10}
\]

\(G_k[n]\) 是截至步首已产生的字节量，未产生时为0。式(10)允许零额外传播延迟，但本时隙新发出的数据也只能在下一步或更晚计为收到。

逐跳因果：

\[
0\le u_{kh}[n]\le A_{k,h-1}[n]-F_{kh}[n],\qquad 0\le F_{kh}[n]\le B_k.
\tag{11}
\]

右端减去已经发送的量，避免重复发送被计作新字节。所有链路共用步首接收状态，禁止同一时隙瞬时多跳。

采用共享时分信道时，对正有效速率的链路：

\[
\sum_{(k,h):e_{kh}\in\mathcal E_c}
\frac{u_{kh}[n]}{C_{e_{kh}}[n]}\le\Delta.
\tag{12}
\]

链路不支持、被遮挡或不在合格服务区间时令对应 \(u=0\)，不进行除零。\(C\) 是该时隙已分配独占链路时的有效速率或保守值，且应覆盖本时隙使用的几何/模式条件。使用下界服务率时可以给出条件性保证，仅用均值预测时只作名义计划。若有效速率已计入丢包，不重复乘丢包率。

接收时刻：

\[
r_k^{\mathrm{receipt}}=\min\{t_n:A_{kH_k}[n]=B_k\}.
\tag{13}
\]

更新计划时保留既有累计传输、接收和在途历史，不能把账本清零；无法收到的远端账本不视为母船已知事实。

### 5.6 完整问题的简式

\[
\boxed{
\min_{y,s,\vartheta,u,T}T
\quad\text{s.t. 任务满足、资源时序、运动/观测可行、有限交付，以及式(1)的终端要求。}
}
\tag{14}
\]

这四组约束就是第一版主体。原子锁、日志身份和模型版本是落实约束的执行机制，不再分别扩展成多组新的优化问题。

---

## 6. 从模型到求解

### 6.1 变量消元与分解

给定离散方法与顺序 \(y\)、活动安排 \(s\) 和运动参数 \(\vartheta\)，平台轨迹与终态由后端确定，观测产生时刻由轨迹评价确定，链路可用性/速率由相对状态和设备模型确定。

因此不需要分别将姿态、每个电机输入、图拉普拉斯、信号强度等都设为主搜索变量。它们仍在物理模型中发挥作用，但不会在任务层重复优化。

定义

\[
V_t(y,s,\vartheta)=\min_uT
\]

为该候选的通信与结束时间评价。整体问题通过方法/顺序搜索、运动查询与传输评价交错进行。固定运动轨迹、产生时刻、路由和时隙服务条件后，式(9)–(12)对传输变量是线性的。对一个试探完成时隙，检查所有必要 \(A_{kH}[N]=B_k\) 是线性可行性问题；在已知有效的有限预测区间内可寻找最早可行接收时刻。

若改变活动开始时刻会改变动力学初态、障碍位置或链路窗口，必须重新评价。只求解一次内层后永久缓存一个时间数不是联合求解。

### 6.2 既有搜索的直接使用

1. 保留已接受承诺和当前实际收到的信息。
2. 对当前任务扩展合格执行方法、成员、顺序及自动生成的入口/支援几何。
3. 查询完整运动和观测；未知不当作已证明不可行。
4. 评价共享通信；如需要等待或更换会合机会，重算受影响的时序和运动。
5. 对完整候选做全平台、全时段检查。
6. 在预算内保存工期最短的已验证完整计划。
7. 仅派发前置满足且能够被相关节点接受的片段。
8. 新事件或条件变化后，更新未承诺后缀。

这不是所有层离散化。离散搜索的是方法与顺序，Swarm等后端仍进行连续轨迹计算。有限空间采样只限制本轮候选范围，不将采样点升级为用户必须使用的固定入口。

局部优化器返回的是可行运动候选，不保证它在给定方法下最小化全任务工期。精确联合最优与预算内候选最优必须分开。

---

## 7. 推导一：完成时间为什么由关键依赖而非各平台耗时求和决定

考虑受限子类：方法、路径、互斥顺序和有效传输活动固定，活动时长不随开始时刻改变，只有完成到启动的下界依赖及固定释放时刻。构造有向无环活动图，为任务接收和最终结束设置零时长事件节点。

对活动 \(v\)：

\[
ES(v)=\max\{r_v,\max_{u\in\operatorname{pred}(v)}EF(u)\},
\qquad EF(v)=ES(v)+d_v.
\tag{15}
\]

设终结点为 \(v_f\)，則 \(T=EF(v_f)\)。将释放时刻编码为源边后，它等于源到终结点的最长加权路径。

### 命题A：固定活动图的最早可行时序

在上述子类中，式(15)得到不晚于任何其他可行时序的活动完成时刻，因而得到该固定活动图的最小工期。

**证明。** 按拓扑序归纳。源活动不可能早于释放时刻。假设每个前驱最早完成时刻不晚于其他任一可行时序，则后继开始必须不早于所有前驱完成和自身释放的最大值，因此不早于式(15)给出的值。加上固定活动时长，结论对后继成立。对最终节点成立，即得最小工期。证毕。

这条结果解释资源/信息耦合，但不适用于任意时变旅行时间或链路窗口。一般情形必须连同运动和通信重新求解，不能将该递推冒充整个非凸联合问题的精确解法。

业务含义：一个平台更快并不一定改善任务工期；只有它缩短了控制最终结束的关键依赖，才改善主目标。编队、AAV入水和中继安排的价值，都应通过完整任务时序评价。

---

## 8. 推导二：什么时候应派USV去中继

先分析一个可解释的局部特例，而不引入额外经验权重。假设一个产品全部产生于 \(g\)，源平台到达可服务位置的时刻为 \(t_w\)，USV出发时刻为 \(t_s\)，从其出发状态至可用服务区域的合格转场时间为 \(\tau_s\)。该区间内链路持续可用、独占、有效速率为常数 \(C\)，接收延迟为 \(\ell\)。

最早接收完成时刻为：

\[
r=\max\{g,t_w,t_s+\tau_s\}+\frac{B}{C}+\ell.
\tag{16}
\]

**推导。** 开始有效发送前，数据、源端和支援端三项条件均应成立，因此开始时刻至少是三者最大值。发送 \(B\) 字节至少需 \(B/C\)，再计接收延迟。在全部假设满足时立即以速率 \(C\) 发送即可达到这个界，故为该特例最早完成时刻。

对已有可行计划，其工期为 \(U\)，从该接收事件到最终任务结束的固定最长后继时长为 \(L_{\rm after}\)。若要不延误这个已有计划，派生接收预算为：

\[
D=U-L_{\rm after}.
\tag{17}
\]

它是计划内部倒推的最晚接收时刻，不是用户凭空输入的业务截止。若相同后继结构与资源安排保持有效，则

\[
\max\{g,t_w\}\le D-\frac{B}{C}-\ell,
\]

\[
\boxed{t_s\le D-\frac{B}{C}-\ell-\tau_s.}
\tag{18}
\]

若右端早于USV最早可出发时刻，该方法不能保持当前工期；应比较更早预定位、其他会合区域、直接交付、其他执行者，或接受更长工期。不是加一个“中继奖励”让USV出发。

一般多产品、多跳、共享容量及时变链路下，式(16)不能逐项套用相加，应用式(9)–(13)求解。该特例用于理解和候选排序，不代替完整校核。

---

## 9. 理论性质一：最小模型中的条件性组合可行性

不再为此引入一份六元组“执行合同”。只明确下列假设：

H1. 每个选定活动的名义轨迹从指定完整初态出发，满足该平台模型、模式、环境、观测和必要终端条件。

H2. 同一平台的活动及等待过程完整衔接；所有时刻均有轨迹定义，且实际用于评价的时间与计划一致。

H3. 完整计划满足物理成员互斥、全平台运动不冲突、任务/接收依赖和式(9)–(13)的共享通信约束。

### 命题B：名义计划组合可行性

在H1–H3下，选定活动的组合满足式(14)的名义任务可行性条件。

**证明。** 逐平台按活动顺序拼接轨迹。H1提供各段模型与局部条件成立；H2保证拼接边界状态一致，空档也有合法演化，不引入瞬移或遗漏运动。H3分别排除跨活动资源双占和跨平台碰撞，并保证活动只在对应前置条件后开始。由式(11)，本时隙的发送量不超过步首先前已得到且未发送的数据；式(12)不超出共享服务预算；式(10)保证接收不会早于发送和声明延迟。故所有最终收到的产品都来自先前产生和合法转发的数据。将这些结果与式(1)的完成条件结合，得到完整名义计划满足全部请求要求。证毕。

这是一条模型级组合结论，不证明任意真实扰动下安全。它的作用是明确每段查询之外还必须检查哪些跨方法关系；一个方法单独合格不能直接被当作完整计划合格。

如果实现只进行离散采样，则本命题只适用于该离散检查模型。若另有安全余量函数 \(q(\tau)\) 的Lipschitz界 \(L_q\)，检查点覆盖区间两端且最大间隔为 \(h\)，则

\[
q(t_n)\ge L_qh/2\quad\forall n
\Longrightarrow q(\tau)\ge0\quad\forall\tau.
\]

证明由任意时刻距离最近检查点不超过 \(h/2\) 得到。这是以后扩展连续保证的途径，不要求v1凭空假定已有可靠跟踪界。

---

## 10. 理论性质二：执行与修复一致性

使用已有活动身份、执行代次、产品身份和物理资源登记；不增加另一个事务系统。

规则R1：只有全部所需物理成员一次性成功预订，活动才被接受。

规则R2：活动只依据匹配身份的实际前驱完成/接收事件推进，不依据预测时间自动宣称完成。

规则R3：终端释放事件必须匹配当前活动与代次；重复事件只生效一次。数据收到与运动资源释放独立。

规则R4：修复保留全部未获有效撤销确认的已接受承诺，新后缀与保留部分联合校核。未开始的已接受会合也算承诺。

### 命题C：事件一致性不变式

若初始物理占用无冲突，登记具有单一权威和原子操作，事件身份与代次校验正确，则R1–R4在任意有限事件序列下保持：同一物理成员不被互斥活动双重占用；旧事件不释放新活动；接收前置不提前生效；重复接收不重复累计；修复不单方覆盖未撤销的承诺。

**证明。** 对事件序列长度归纳。初始状态成立。接纳事件只在全部相关成员可用时登记，保持互斥；开始事件检查真实前置，保持因果；结果/接收事件验证当前身份和代次，并以幂等方式更新，不改变其他活动的占用；修复事件只修改未承诺部分，且新后缀先经联合检查，故保留旧承诺并不引入双占。穷举这些状态转移即可得到结论。证毕。

新障碍可能使物理承诺无法安全继续。此时不能为了“保持承诺”继续执行失效路线，应采用既有合格安全处置并登记失败/待协调状态。本命题保证账本和事件一致，不保证任意环境变化下的物理安全或最终完成。

有限完成还需要有限任务/补查、无循环依赖、动作及合格等待有界、必要通信可获得足量服务、公平执行和不会无限次重规划而不推进等假设。v1 不把这些压缩成“DAG无环所以一定完成”。

---

## 11. 理论性质三：候选范围内的解质量

令 \(\mathcal P_D\) 为声明的有限完整候选计划空间；该空间包括入口/路线/方法/时序候选范围及后端返回规则，而不是只有“平台分配”这一维度。假设可行性判定正确。

令 \(\mathcal P_{\rm found}\) 为预算内完成验证的集合：

\[
U=\min_{\Pi\in\mathcal P_{\rm found}}T(\Pi),
\quad
T_D^*=\min_{\Pi\in\mathcal P_D\cap\mathcal F_t}T(\Pi).
\]

### 命题D：候选最优与任意时刻解保留

完整枚举全部候选且正确评价所有候选时，返回 \(T_D^*\)。预算提前结束时，保留的 \(U\) 是当前最好已知完整可行解；在相同信息快照和相同候选问题内继续搜索并保留旧解，\(U\) 单调不增加。

**证明。** 完整枚举不会漏掉候选空间中的更优可行方案；每次只以更低工期替换旧解，得到单调性。证毕。

若对每个未展开子树有合法下界 \(L(n)\)，并且未展开集合仍覆盖所有尚未排除的候选，则

\[
L=\min\{U,\min_{n\in\mathrm{frontier}}L(n)\},\qquad
L\le T_D^*\le U.
\]

此时 \(U-L\) 才是有效的绝对差距。没有这样的下界，不报告最优性差距；某次可行轨迹的耗时一般是一个上界，不是下界。

环境改变后旧解可能不可行，跨轮次不承诺工期单调变好。局部轨迹优化失败/超时不构成整个方法无解证明。若只有采样可行性判定，最优性也仅限定在该采样模型，不得写成原连续物理问题的全局最优。

---

## 12. 如何体现方法贡献与验证

这些基础命题本身不应包装成新控制理论。研究增量应由“跨介质执行方法、有限交付、共享成员和实际反馈共同改变计划”的机制与实验支持。

保留少量直接对应推导的验证：

1. 同一任务下，改变一处可行入口或转换代价，检查是否跨越候选优劣边界。
2. 固定请求，比较UUV包办与AAV分担水下任务对空中剩余任务及整项工期的影响。
3. 改变有效服务率或USV可用时刻，检查式(18)预测的支援时序变化，以及完整传输评价是否一致。
4. 方法分别安全/可交付而组合碰撞/容量不足时，完整计划必须拒绝。
5. 一次真实延迟或收到的覆盖缺口触发后缀更新，检查已接受会合与物理占用不被错误覆盖。
6. 小实例穷举对账有限候选最优；大实例同预算比较完整可行率与实际完工时间。

失败基线没有满足业务要求时，不能仅凭其预测时间短就作为更优成功结果。没有编队业务收益时允许独立执行获选，编队能力仍保留明确的执行验收。

---

## 13. 可直接使用的方法总述

本文将异构无人集群的有限监测任务建模为任务方法、资源时序、跨介质运动及有限交付的混合离散—连续规划问题。在给定平台能力、当前可用状态和环境信息的条件下，规划器选择执行方法及物理成员，并联合确定活动时序、入出水与会合几何以及必要传输安排，以最小化全部规定作业、结果接收与终端行为的结束时刻。各平台既有运动与控制后端用于评价候选活动的完整耗时、终态和轨迹，不在上层重复优化控制器内部参数。所选活动在接受前统一检查物理资源、全平台运动和共享通信约束。执行阶段使用实际完成与接收事件更新任务，在保持有效已接受承诺的条件下重新规划未承诺后缀。

以上总述描述方法设计；论文中的实现、证明和实验应分别标示，不把模型中写出的条件当作当前源码已经全部实现。

---

## 14. 主要文献及采用范围

[1] Messing, A., Neville, G., Chernova, S., Hutchinson, S., Ravichandar, H. GRSTAPS: Graphically Recursive Simultaneous Task Allocation, Planning, and Scheduling. IJRR, 41(2), 2022. DOI: 10.1177/02783649211052066. https://journals.sagepub.com/doi/10.1177/02783649211052066

采用：任务不预绑定成员、分配/时序/运动交错反馈。边界：不继承其原实现不处理机器人间碰撞的安全假设，也不将原来的旅行时间近似当作qn/PVS完整执行成本。

[2] Quan, L., Yin, L., Xu, C., Gao, F. Distributed Swarm Trajectory Optimization for Formation Flight in Dense Environments. ICRA 2022. https://arxiv.org/html/2109.07682v2

采用：保留MINCO连续轨迹优化、原编队相似度与运动后端。边界：原软惩罚和样本检查不自动变成当前所有平台的连续安全证明；底层权重不是高层业务奖励。

[3] Guo, M., Zavlanos, M. M. Multi-Robot Data Gathering Under Buffer Constraints and Intermittent Communication. IEEE T-RO, 34(4), 2018. DOI: 10.1109/TRO.2018.2830370. https://arxiv.org/html/1706.02092

采用：数据采集与中继提前安排会合、执行承诺。边界：原任务独立且预分配，初始中继到中心立即上传假设须替换。

[4] Zhang, X., Chen, J., Zhu, Y., Luo, B., Guo, M. CoCoPlan: Adaptive Coordination and Communication for Multi-robot Systems in Dynamic and Unknown Environments. arXiv:2601.10116. https://arxiv.org/html/2601.10116v1

采用：任务选择和通信事件共同搜索。边界：不照搬全队连通事件、适应性加权目标或其最优性论断；有限逐跳交付另行建模。

[5] Huang et al. APEX-MR: Multi-Robot Asynchronous Planning and Execution for Cooperative Assembly. RSS 2025. https://arxiv.org/html/2503.15836v2

采用：实际执行进展释放后继，而非固定计时。边界：机械臂等待与运动假设不能直接推广到欠驱动水下平台。

本文公式(1)–(18)及命题A–D为针对本项目给出的精简建模与条件性推导，不是上述论文的原式或原定理复述。其工程实现和原创性仍需由对应代码与同条件实验验证。
