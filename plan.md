# Heformation 三类平台协同任务实施计划

> 当前进度（2026-09-24）：原港口UUV＋移动USV同一正式请求组件已实际完成观测、有限收件、双Action终态、规定返回及实时RViz/中文面板；五平台整场时间/几何适用项合格。三类同请求在测试性排除AAV水下替代法后已得到完整AAV AIR＋UUV WATER＋USV计划，但360s与180s诊断规划均使本机qn规划期模型/ROS漂移约1.7s>原0.05s，AIR Action未派发，真实请求UNKNOWN_LOCKED；失败和前述组件正例并列保留。普通完整能力请求仍选AAV跨介质、UUV待命；最终有业务依据的UUV角色、初次规划政策、一次真实复查/在线修复与三类同次全程有效可视化验收未完成。详见WORKLOG顶部与context/02。

## 1. 目标、依据与默认选择

完成**一项近岸空中—水下联合观测任务**：用户提交作业要求，系统自主选择观测方法、执行平台、入／出水位置、任务顺序、通信支援及编队安排；必要结果到达母船后，按既定规则最多触发一轮复查，最终完成规定返回或明确报告失败。

采用统一目标：

\[
\min T_{\mathrm{end}}
=\min\max\{\text{必做作业完成时刻、必要结果接收时刻、规定返回完成时刻}\}.
\]

这是统一决策问题，**不把全部轨迹、姿态和推进器输入放进一个巨大优化器**。沿用现有候选搜索，交错进行方法选择、时序安排、运动查询和有限交付校核。

固定以下选择：

- 有限观测视点／通过段参与联合选择，不在分配前用贪心方法永久固定全部观测任务。
- AAV 的 AIR／WATER 能力来自平台配置与资格记录；入水时机由系统选择，不要求用户默认勾选。
- 无业务期限时 `deadline=None`；业务截止、求解预算和执行观察超时分别处理。
- 主任务自动比较独立和编队方法；另设同场景编队能力验收，不制造奖励强迫优化器选择编队。
- 使用现有 Swarm＋qn＋PVS、ROS 1、Docker 和 RViz。VRX 保留为独立试验，不进入本轮必要运行链。
- 通信使用声明式实验模型，不宣称设备性能。无可靠能量模型时明确记录未评价，不编造能耗最优结论。
- 源码在 `main` 管理，旧三机／七机入口及历史失败保留。

直接依据为 **GRSTAPS（IJRR 2022）的交错规划、D-ITAGS（RA-L 2023）的运动反馈与针对性修复、Calvo（T-RO 2025）的调度与执行修复、Guo–Zavlanos（T-RO 2018）和 CoCoPlan（RA-L 2026）的通信会合、APEX-MR（RSS 2025）的实际事件释放，以及 Swarm（ICRA 2022）的编队运动优化**。采用机制与不能继承的假设统一记录在现有[协同设计依据](/home/lhj/Swarm-Formation/docs/requirements/cooperation-source-design-20260920.md)，不接入这些论文的整套框架。

## 2. 统一任务、活动与求解

### 业务要求与执行活动

保留现有请求入口，表达区域／样点、观测条件、接收方、复查规则和返回要求。用户不指定机器人编号、入水时刻或 USV 出发顺序。

一个业务任务可以展开为多个活动：

- 单个平台内部：转场→转换→观测→终端等顺序步骤。
- 多平台协作：水下平台作业与 USV 转场分别安排，随后在共同通信区间配合。
- 数据依赖：由指定接收端的接收事件释放，不由源端观测完成代替。

调整现有 Plan：业务任务 ID 可关联多个唯一执行活动 ID；成员占用按各自实际活动区间记录。取消“每个业务任务只能有一个 PlanItem”和“计划等待必须为零”的限制。

`execution_steps` 成为选定方法的唯一执行来源。旧单步表示仅在一个兼容边界转换，不继续双写 `native_action` 与另一份步骤数据。内部模型副本、密集轨迹和诊断历史不放进每个 Goal。

### 有限候选与时序搜索

在现有 `build_executor_plan()` 内完成以下过程，不新增在线求解器：

1. 读取同一版本的平台状态、资格、占用、已接受承诺、场景和已接收结果。
2. 生成有限观测视点／通过段，记录它们能满足哪些作业要求；覆盖约束参与方法选择。
3. 比较合格的 AAV／UUV 方法、转换入口、路线、直接交付／USV 支援，以及独立／编队运动。
4. 联合安排活动顺序和有限等待选择，包括立即出发后等待、延迟出发、换通信机会或换方法。
5. 按需调用已有运动后端，返回可执行性、完整耗时与终端状态；结果改变后重新传播受影响时序。
6. 校核成员占用、空间冲突、通信重叠、数据接收与返回，保存完整可行计划。

每次初始规划或修复共用 **10 秒单调预算**，所有查询消耗剩余预算。优先取得完整可行解，再改进工期；预算耗尽保留已有完整解，没有完整解则如实报告。未知查询不等于无解。

缓存限定于相同状态、模式、模型、环境和承诺条件。未经证明的耗时估计只能用于排序，不用于最优性剪枝。最优性声明仅限实际穷尽的有限候选模型。

### 完整成本与资源关系

- 转场、转换、观测、必要等待、交付和终端各计一次；返回从最后相关活动的实际／预测终态计算。
- UUV 观测与 USV 转场可以重叠，不能将双方耗时简单相加。
- 不把协作方法的所有参与者从最早开始一直锁到最晚结束；分别预订成员活动区间及已接受片段。
- 任务、成员、转换区和冲突路段的先后关系联合查环；支援重叠不能表达成双方互等完成。
- 修复可改变未承诺部分的平台和方法；已接受的未来会合也属于承诺，未经通知确认不得单方取消。

## 3. 运动、编队、通信与实际执行

### 先解决有效执行基础

首先复现并定位现有时间漂移、状态过期及显示负载问题。积分时间只累计真实完成的积分步骤，不能改时间戳、重置累计时间或放宽阈值来通过验收。显示与日志不得持有执行锁进行阻塞操作。

继续使用共同静态近岸场景：水面、海底、禁入区和有限实体障碍。复用 Swarm 的障碍生成方式，保持显示、规划点云和安全几何一致。障碍必须实际影响至少一条路线、转换入口或通过顺序；不新增写实资产、海浪研究或动态交通模型。

### 运动与编队职责

- AIR 继续使用现有 Swarm；通过同源、只读的运动查询调用原优化代码，不向实际轨迹话题发送“试探目标”。查询受已有预算隔离约束。
- qn／PVS 使用实际模型状态副本校核完整方法，包含转换、转弯、尾段和等待。没有取得资格的方法返回未知或拒绝。
- 同型号 AAV 复用相同资格实现，但逐成员核对入口和状态；不永久绑定某台 AAV 为水下专员。
- 编队候选包含各成员集结、最慢成员等待、Swarm 编队段及解散后的状态；停止在巡查末尾无条件追加编队。
- Swarm 原生图项与内部优化权重保留，不另写编队控制器；USV/UUV 不加入 AAV 编队图。
- 全平台运动冲突校核覆盖完整占用过程。冲突优先通过候选路线和活动顺序消解，在线实际违规仍进入既有故障处理。欠驱动平台不能在冲突边界被假定为瞬时停止。

### 预承诺与有限交付

断联前，各必要平台先接纳有限本地片段，片段覆盖作业、通信机会、终端和失约处置。全部必要接纳证据齐备后，才释放依赖该承诺的执行。USV 的预定位不等待必须经它转发的水下完成通知。

接口增量限定为：

- 保持 `Formation.action`。
- 在现有 `PlatformTask` 增加有限片段预装载及相应反馈语义。
- 为已预装载片段提供一个带 GoalID／代次的幂等启动入口，直接挂在原本地执行端；不新建协调节点。
- 产品事件仅携带身份、来源、产生时刻、作业结果与所需数据量；不传整份模型或证据历史。

接纳不等于启动，启动不等于动作完成。某方拒绝、超时或启动状态未知时，不进入依赖它的断联段；已接受成员按声明处置并保留承诺／锁定。

复用 `FiniteDelivery`，按共同通信步首状态一次更新所有信道，禁止通过调用顺序产生同一时间步的瞬时多跳转发。链路取决于实际位置、介质及声明几何；各产品共享实际容量，断链零增量、恢复续传、重复消息不重复计数。

首个实验配置固定为：每份观测摘要 **32 KiB**；水下链路有效范围 **8 m**、共享有效速率 **2 KiB/s**；空中／水面到母船链路范围 **30 m**、共享有效速率 **32 KiB/s**；使用既有 **0.1 秒**时间网格。通知按实际编码长度占用容量。以上仅为可重复的实验假设，不称为硬件指标；不另外建立声学传播或协议栈。

本地观测产生产品，仿真传输边界处理发送与接收，母船任务程序只消费收到的事件。独立评测可以读取真值，调度器不能从 bag、共享证据或显示代理补齐未收到的信息。

### runner 与界面

在现有 worker 中执行复合步骤，处理对应 GoalID 的反馈和结果；子步骤失败即阻断后继。同一成员的连续片段在规定终端前不释放。

请求级成功需要必做观测、必要接收、已激活复查及规定返回全部实际完成。复查最多一轮，只能由已收到的结果触发。

复用现有 Qt／RViz：展示实际候选计划、选择理由、等待原因、接收进度、成员占用和失败。确认对应具体计划版本；未确认不派发，运行中不接受新批次，关闭 UI 不结束执行。

## 4. 实施顺序与最小必要验证

按依赖顺序实施，每步只运行能证明该变化的检查，不按函数数量补测试，也不展开参数穷举。

| 顺序 | 交付 | 必要验证 |
|---|---|---|
| 1 | 时间可靠性、请求可选期限、活动与步骤语义收敛 | 原失败复现与修正；无截止请求可规划；同一业务多活动、合法等待可表达，重复占用被拒绝 |
| 2 | 有限观测候选、AAV/UUV 完整方法、复合派发 | 同需求改变入口可行性或转换代价后选择改变；实际跨介质和返回成功；失败子步骤不继续 |
| 3 | USV 支援与有限交付进入同一计划 | UUV 作业与 USV 转场确实并行；通知不可达仍执行已承诺会合；未收齐结果不释放复查 |
| 4 | 编队方法进入比较 | 同作业要求下比较完整独立／编队成本；无收益时允许独立；另完成 Swarm 编队能力实跑 |
| 5 | 一项完整请求和代表性失败 | 同一次 UI 确认贯通分配、跨介质、支援、接收、复查及返回；同时保存时间、安全和资源证据 |

只补会改变正确性结论的确定性测试：旧 Result 不解锁、缺少参与方接纳不启动、跨信道步首因果、运动先后成环、预算耗尽保留承诺。其余以组件边界实跑和完整请求证据为主。

科研对照使用同一作业要求、物理和通信评价条件，比较串行／联合安排、忽略／计入转换、理想／有限交付、固定／反馈修复。用小规模穷尽参考评价有限候选内解质量；不允许失败基线仅凭预测更快就记作成功。

## 5. 交付、理论边界与代码约束

最终交付包括：一份可加载的联合观测请求、一套共同障碍场景、现有 runner 上的完整协作执行、中文实时界面、成功与代表性失败记录、方法对照，以及明确假设下的理论论证。

理论分别处理：

- 物理成员不重复占用、未接收数据不提前释放、旧事件不破坏新承诺。
- 在联合依赖无环、动作与终端有界、通信可实现且容量足够等条件下的执行进展。
- 规划安全到实际安全的条件性传递；经验误差不冒充严格跟踪界。
- 有限候选模型中的解质量；不继承其他论文未经满足的最优性或稳定性结论。

运行结构始终保持：

**现有请求／调度／runner → 原生 Action → Swarm／qn／PVS → 实际执行与接收事件。**

不增加第二套任务状态、后端工厂、通用工作流引擎、统一执行信封或多层协调服务。模型参数加载一次；内部查询结果不反复序列化；只为本次真实消费者保留字段。必要的成员身份、GoalID、模式、坐标、参考归属和数据接收语义不删除。

每次修改或实验在 WORKLOG 顶部记录操作、结果、失败、证据位置和下一步；阶段结束更新当前状态与交接。编队、跨介质或通信交付未通过时明确保留未完成，不通过降低要求或删掉交付宣布完成。
